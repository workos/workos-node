// This file is auto-generated by oagen. Do not edit.

import fetch from 'jest-fetch-mock';
import {
  fetchOnce,
  fetchURL,
  fetchMethod,
  fetchBody,
  testUnauthorized,
} from '../common/utils/test-utils';
import { WorkOS } from '../workos';

import applicationCredentialsListItemFixture from './fixtures/application-credentials-list-item.fixture.json';
import newConnectApplicationSecretFixture from './fixtures/new-connect-application-secret.fixture.json';

const workos = new WorkOS('sk_test_Sz3IQjepeSWaI4cMS4ms4sMuU');

describe('ApplicationClientSecrets', () => {
  beforeEach(() => fetch.resetMocks());

  describe('list', () => {
    it('returns the expected result', async () => {
      fetchOnce(applicationCredentialsListItemFixture);

      const result = await workos.applicationClientSecrets.list('test_id');

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/connect/applications/test_id/client_secrets',
      );
      expect(Array.isArray(result)).toBe(true);
      expect(result).toHaveLength(1);
      expect(result[0].object).toBe('connect_application_secret');
      expect(result[0].id).toBe('secret_01J9Q2Z3X4Y5W6V7U8T9S0R1Q');
      expect(result[0].secretHint).toBe('abc123');
      expect(result[0].lastUsedAt).toBe(null);
      expect(result[0].createdAt).toBe('2026-01-15T12:00:00.000Z');
      expect(result[0].updatedAt).toBe('2026-01-15T12:00:00.000Z');
    });

    testUnauthorized(() => workos.applicationClientSecrets.list('test_id'));
  });

  describe('create', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(newConnectApplicationSecretFixture);

      const result = await workos.applicationClientSecrets.create(
        'test_id',
        {},
      );

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/connect/applications/test_id/client_secrets',
      );
      expect(fetchBody()).toBeDefined();
      expect(result.object).toBe('connect_application_secret');
      expect(result.id).toBe('secret_01J9Q2Z3X4Y5W6V7U8T9S0R1Q');
      expect(result.secretHint).toBe('abc123');
      expect(result.lastUsedAt).toBe(null);
      expect(result.createdAt).toBe('2026-01-15T12:00:00.000Z');
      expect(result.updatedAt).toBe('2026-01-15T12:00:00.000Z');
      expect(result.secret).toBe(
        'abc123def456ghi789jkl012mno345pqr678stu901vwx234yz',
      );
    });

    testUnauthorized(() =>
      workos.applicationClientSecrets.create('test_id', {}),
    );

    it('throws UnprocessableEntityException on 422', async () => {
      fetchOnce('', { status: 422 });
      await expect(
        workos.applicationClientSecrets.create('test_id', {}),
      ).rejects.toThrow();
    });
  });

  describe('delete', () => {
    it('sends a DELETE request', async () => {
      fetchOnce({}, { status: 204 });

      await workos.applicationClientSecrets.delete('test_id');

      expect(fetchMethod()).toBe('DELETE');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/connect/client_secrets/test_id',
      );
    });
  });
});
