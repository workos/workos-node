// This file is auto-generated by oagen. Do not edit.

import type {
  UserSessionsImpersonator,
  UserSessionsImpersonatorResponse,
} from './user-sessions-impersonator.interface';
import type { UserSessionsAuthMethod } from '../../common/interfaces/user-sessions-auth-method.interface';
import type { UserSessionsStatus } from '../../common/interfaces/user-sessions-status.interface';

export interface UserSessionsListItem {
  /** Distinguishes the session object. */
  object: 'session';
  /** The unique ID of the session. */
  id: string;
  /** Information about the impersonator if this session was created via impersonation. */
  impersonator?: UserSessionsImpersonator;
  /** The IP address from which the session was created. */
  ipAddress: string | null;
  /** The ID of the organization this session is associated with. */
  organizationId?: string;
  /** The user agent string from the device that created the session. */
  userAgent: string | null;
  /** The ID of the user this session belongs to. */
  userId: string;
  /** The authentication method used to create this session. */
  authMethod: UserSessionsAuthMethod;
  /** The current status of the session. */
  status: UserSessionsStatus;
  /** The timestamp when the session expires. */
  expiresAt: string;
  /** The timestamp when the session ended. */
  endedAt: string | null;
  /** An ISO 8601 timestamp. */
  createdAt: string;
  /** An ISO 8601 timestamp. */
  updatedAt: string;
}

export interface UserSessionsListItemResponse {
  object: 'session';
  id: string;
  impersonator?: UserSessionsImpersonatorResponse;
  ip_address: string | null;
  organization_id?: string;
  user_agent: string | null;
  user_id: string;
  auth_method: UserSessionsAuthMethod;
  status: UserSessionsStatus;
  expires_at: string;
  ended_at: string | null;
  created_at: string;
  updated_at: string;
}
