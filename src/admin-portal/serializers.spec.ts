// This file is auto-generated by oagen. Do not edit.

import { serializeSSOIntentOptions } from './serializers/sso-intent-options.serializer';
import { serializeDomainVerificationIntentOptions } from './serializers/domain-verification-intent-options.serializer';
import { serializeIntentOptions } from './serializers/intent-options.serializer';
import { serializeGenerateLink } from './serializers/generate-link.serializer';
import { deserializePortalLinkResponse } from './serializers/portal-link-response.serializer';
import type { SSOIntentOptionsResponse } from './interfaces/sso-intent-options.interface';
import type { DomainVerificationIntentOptionsResponse } from './interfaces/domain-verification-intent-options.interface';
import type { IntentOptionsResponse } from './interfaces/intent-options.interface';
import type { GenerateLinkResponse } from './interfaces/generate-link.interface';
import type { PortalLinkResponseWire } from './interfaces/portal-link-response.interface';
import ssoIntentOptionsFixture from './fixtures/sso-intent-options.json';
import domainVerificationIntentOptionsFixture from './fixtures/domain-verification-intent-options.json';
import intentOptionsFixture from './fixtures/intent-options.json';
import generateLinkFixture from './fixtures/generate-link.json';
import portalLinkResponseFixture from './fixtures/portal-link-response.json';

describe('SSOIntentOptionsSerializer', () => {
  it('serializes correctly', () => {
    const fixture = ssoIntentOptionsFixture as SSOIntentOptionsResponse;
    const serialized = serializeSSOIntentOptions(fixture as any);
    expect(serialized).toBeDefined();
  });
});

describe('DomainVerificationIntentOptionsSerializer', () => {
  it('serializes correctly', () => {
    const fixture =
      domainVerificationIntentOptionsFixture as DomainVerificationIntentOptionsResponse;
    const serialized = serializeDomainVerificationIntentOptions(fixture as any);
    expect(serialized).toBeDefined();
  });
});

describe('IntentOptionsSerializer', () => {
  it('serializes correctly', () => {
    const fixture = intentOptionsFixture as IntentOptionsResponse;
    const serialized = serializeIntentOptions(fixture as any);
    expect(serialized).toBeDefined();
  });
});

describe('GenerateLinkSerializer', () => {
  it('serializes correctly', () => {
    const fixture = generateLinkFixture as GenerateLinkResponse;
    const serialized = serializeGenerateLink(fixture as any);
    expect(serialized).toBeDefined();
  });
});

describe('PortalLinkResponseSerializer', () => {
  it('deserializes correctly', () => {
    const fixture = portalLinkResponseFixture as PortalLinkResponseWire;
    const deserialized = deserializePortalLinkResponse(fixture);
    expect(deserialized).toBeDefined();
  });
});
