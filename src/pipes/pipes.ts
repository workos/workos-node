// This file is auto-generated by oagen. Do not edit.

import type { WorkOS } from '../workos';
import type { ListDataIntegrationsOptions } from './interfaces/list-data-integrations-options.interface';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { CreateDataIntegrationOptions } from './interfaces/create-data-integration-options.interface';
import type { GetDataIntegrationOptions } from './interfaces/get-data-integration-options.interface';
import type { UpdateDataIntegrationOptions } from './interfaces/update-data-integration-options.interface';
import type { DeleteDataIntegrationOptions } from './interfaces/delete-data-integration-options.interface';
import type { UpdateDataIntegrationApiKeyOptions } from './interfaces/update-data-integration-api-key-options.interface';
import type { AuthorizeDataIntegrationOptions } from './interfaces/authorize-data-integration-options.interface';
import type { UpdateDataIntegrationClientCredentialsOptions } from './interfaces/update-data-integration-client-credentials-options.interface';
import type { CreateDataIntegrationCredentialOptions } from './interfaces/create-data-integration-credential-options.interface';
import type { GetOrganizationDataIntegrationOptions } from './interfaces/get-organization-data-integration-options.interface';
import type { UpdateOrganizationDataIntegrationOptions } from './interfaces/update-organization-data-integration-options.interface';
import type { DeleteOrganizationDataIntegrationOptions } from './interfaces/delete-organization-data-integration-options.interface';
import type { GetAccessTokenOptions } from './interfaces/get-access-token-options.interface';
import type { GetOrganizationConnectedAccountOptions } from './interfaces/get-organization-connected-account-options.interface';
import type { CreateOrganizationConnectedAccountOptions } from './interfaces/create-organization-connected-account-options.interface';
import type { UpdateOrganizationConnectedAccountOptions } from './interfaces/update-organization-connected-account-options.interface';
import type { DeleteOrganizationConnectedAccountOptions } from './interfaces/delete-organization-connected-account-options.interface';
import type { ListOrganizationDataProvidersOptions } from './interfaces/list-organization-data-providers-options.interface';
import type { GetUserConnectedAccountOptions } from './interfaces/get-user-connected-account-options.interface';
import type { CreateUserConnectedAccountOptions } from './interfaces/create-user-connected-account-options.interface';
import type { UpdateUserConnectedAccountOptions } from './interfaces/update-user-connected-account-options.interface';
import type { DeleteUserConnectedAccountOptions } from './interfaces/delete-user-connected-account-options.interface';
import type { ListUserDataProvidersOptions } from './interfaces/list-user-data-providers-options.interface';
import type {
  DataIntegration,
  DataIntegrationResponse,
} from './interfaces/data-integration.interface';
import type {
  ConnectedAccount,
  ConnectedAccountResponse,
} from './interfaces/connected-account.interface';
import type {
  DataIntegrationAuthorizeUrlResponse,
  DataIntegrationAuthorizeUrlResponseWire,
} from './interfaces/data-integration-authorize-url-response.interface';
import type {
  DataIntegrationCredentialsResponse,
  DataIntegrationCredentialsResponseWire,
} from './interfaces/data-integration-credentials-response.interface';
import type {
  DataIntegrationAccessTokenResponse,
  DataIntegrationAccessTokenResponseWire,
} from './interfaces/data-integration-access-token-response.interface';
import type {
  DataIntegrationsListResponse,
  DataIntegrationsListResponseWire,
} from './interfaces/data-integrations-list-response.interface';
import type { CreateDataIntegrationResponse } from './interfaces/create-data-integration.interface';
import type { UpdateDataIntegrationResponse } from './interfaces/update-data-integration.interface';
import type { DataIntegrationsUpsertApiKeyRequestResponse } from './interfaces/data-integrations-upsert-api-key-request.interface';
import type { DataIntegrationsGetDataIntegrationAuthorizeUrlRequestResponse } from './interfaces/data-integrations-get-data-integration-authorize-url-request.interface';
import type { DataIntegrationsUpsertClientCredentialsRequestResponse } from './interfaces/data-integrations-upsert-client-credentials-request.interface';
import type { DataIntegrationsVendCredentialsRequestResponse } from './interfaces/data-integrations-vend-credentials-request.interface';
import type { DataIntegrationsGetUserTokenRequestResponse } from './interfaces/data-integrations-get-user-token-request.interface';
import type { OrganizationConnectedAccountResponse } from './interfaces/organization-connected-account.interface';
import type { ConnectedAccountInputResponse } from './interfaces/connected-account-input.interface';
import { deserializeDataIntegration } from './serializers/data-integration.serializer';
import { deserializeConnectedAccount } from './serializers/connected-account.serializer';
import { deserializeDataIntegrationAuthorizeUrlResponse } from './serializers/data-integration-authorize-url-response.serializer';
import { deserializeDataIntegrationCredentialsResponse } from './serializers/data-integration-credentials-response.serializer';
import { deserializeDataIntegrationAccessTokenResponse } from './serializers/data-integration-access-token-response.serializer';
import { deserializeDataIntegrationsListResponse } from './serializers/data-integrations-list-response.serializer';
import { serializeCreateDataIntegration } from './serializers/create-data-integration.serializer';
import { serializeUpdateDataIntegration } from './serializers/update-data-integration.serializer';
import { serializeDataIntegrationsUpsertApiKeyRequest } from './serializers/data-integrations-upsert-api-key-request.serializer';
import { serializeDataIntegrationsGetDataIntegrationAuthorizeUrlRequest } from './serializers/data-integrations-get-data-integration-authorize-url-request.serializer';
import { serializeDataIntegrationsUpsertClientCredentialsRequest } from './serializers/data-integrations-upsert-client-credentials-request.serializer';
import { serializeDataIntegrationsVendCredentialsRequest } from './serializers/data-integrations-vend-credentials-request.serializer';
import { serializeDataIntegrationsGetUserTokenRequest } from './serializers/data-integrations-get-user-token-request.serializer';
import { serializeOrganizationConnectedAccount } from './serializers/organization-connected-account.serializer';
import { serializeConnectedAccountInput } from './serializers/connected-account-input.serializer';

export class Pipes {
  constructor(private readonly workos: WorkOS) {}

  /**
   * List data integrations
   *
   * Lists the environment's data integrations configured with `custom` or `organization` credentials, including custom providers and API key integrations. Both user-owned and organization-owned roots are returned, each as its own row with an `ownership`; filter with `ownership` to return only one kind.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<DataIntegration, ListDataIntegrationsOptions>>}
   * @throws {UnauthorizedException} 401
   * @throws {NotFoundException} 404
   * @throws {UnprocessableEntityException} 422
   */
  async listDataIntegrations(
    options?: ListDataIntegrationsOptions,
  ): Promise<AutoPaginatable<DataIntegration, ListDataIntegrationsOptions>> {
    const paginationOptions = options;
    return new AutoPaginatable(
      await fetchAndDeserialize<DataIntegrationResponse, DataIntegration>(
        this.workos,
        '/data-integrations',
        deserializeDataIntegration,
        paginationOptions,
      ),
      (params) =>
        fetchAndDeserialize<DataIntegrationResponse, DataIntegration>(
          this.workos,
          '/data-integrations',
          deserializeDataIntegration,
          params,
        ),
      paginationOptions,
    );
  }

  /**
   * Create a data integration
   *
   * Creates a data integration for a provider. Set `credentials.type` to `custom` to use your own OAuth app credentials or `organization` to have each organization supply its own. Set `auth_methods` to `["api_key"]` to create an API key integration; you may optionally supply an `api_key` block to install a first tenant in the same call. Set `auth_methods` to `["client_credentials"]` to create a client-credentials integration; client credentials are installed per-tenant afterwards. Set `ownership` to `organization` to create the integration organizations connect to instead of the default user-owned one; a provider may have one of each. For a built-in provider, pass its slug as `provider`. For a custom provider, pass a new slug plus a `custom_provider` definition, or the slug of an existing custom provider (without `custom_provider`) to add the other ownership.
   * @param options - Object containing provider.
   * @param options.provider - The provider to create a Data Integration for. For a built-in provider use its slug (e.g. `github`, `slack`). For a custom provider, this is the new provider slug and `custom_provider` must be supplied. A custom provider slug cannot shadow an existing global provider slug.
   * @example "github"
   * @param options.ownership - Who owns the Data Integration. `user` (the default) creates the integration users connect their own accounts to; `organization` creates the root organizations connect to. Ownership is fixed at creation, and one integration of each ownership may exist per provider. Independent of `credentials.type`.
   * @example "user"
   * @param options.description - An optional description of the Data Integration.
   * @example "Production GitHub app"
   * @param options.enabled - Whether the Data Integration is enabled. Defaults to `false`.
   * @example true
   * @param options.scopes - The OAuth scopes to request for the Data Integration. Defaults to the provider's configured scopes when omitted.
   * @example ["repo","read:org"]
   * @param options.authMethods - How accounts authenticate with the provider. Defaults to `["oauth"]`. Use `["api_key"]` to declare an API key integration; `credentials` is then not required and keys are supplied per-tenant (optionally via `api_key` on this request). Use `["client_credentials"]` to declare a client-credentials integration; `credentials` is likewise not required and client credentials are supplied per-tenant.
   * @example ["oauth"]
   * @param options.config - Provider-specific config values (e.g. a Snowflake `account`), keyed by the config field. Only fields the built-in provider declares are accepted.
   * @example {"account":"myorg-myaccount"}
   * @param options.credentials - The OAuth credentials to configure for the Data Integration. Required for OAuth integrations; omit when `auth_methods` is `["api_key"]`.
   * @param options.apiKey - An optional API key to install for the first tenant on an `api_key` integration. Omit to declare a keyless integration; tenants can be added later via the per-installation API key path.
   * @param options.customProvider - The OAuth definition for a custom provider. Supply this to define a custom provider; omit it to create an integration for a built-in provider.
   * @returns {Promise<DataIntegration>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   */
  async createDataIntegration(
    options: CreateDataIntegrationOptions,
  ): Promise<DataIntegration> {
    const payload = options;
    const { data } = await this.workos.post<
      DataIntegrationResponse,
      CreateDataIntegrationResponse
    >('/data-integrations', serializeCreateDataIntegration(payload));
    return deserializeDataIntegration(data);
  }

  /**
   * Get a data integration
   *
   * Retrieves the user-owned data integration by its slug.
   * @param options - The request options.
   * @param options.slug - The slug identifier of the data integration.
   * @example "github"
   * @returns {Promise<DataIntegration>}
   * @throws {UnauthorizedException} 401
   * @throws {NotFoundException} 404
   */
  async getDataIntegration(
    options: GetDataIntegrationOptions,
  ): Promise<DataIntegration> {
    const { slug } = options;
    const { data } = await this.workos.get<DataIntegrationResponse>(
      `/data-integrations/${encodeURIComponent(slug)}`,
    );
    return deserializeDataIntegration(data);
  }

  /**
   * Update a data integration
   *
   * Updates the description, enabled state, or custom credentials of the user-owned data integration. For custom providers, `custom_provider` updates the OAuth definition.
   * @param options - The request body.
   * @param options.slug - The slug identifier of the data integration.
   * @example "github"
   * @param options.description - An optional description of the Data Integration.
   * @example "Production GitHub app"
   * @param options.enabled - Whether the Data Integration is enabled.
   * @example true
   * @param options.scopes - The OAuth scopes to request for the Data Integration. Pass `null` to reset to the provider's configured scopes.
   * @example ["repo","read:org"]
   * @param options.credentials - New OAuth credentials for the Data Integration. When provided, rotates the stored client secret. Mutually exclusive with `api_key`.
   * @param options.apiKey - An API key to install or rotate for a tenant on an `api_key` integration. Upserts the tenant installation identified by `user_id` (and optional `organization_id`).
   * @param options.customProvider - Updates to a custom provider's OAuth definition. Only valid for custom-provider integrations.
   * @returns {Promise<DataIntegration>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   */
  async updateDataIntegration(
    options: UpdateDataIntegrationOptions,
  ): Promise<DataIntegration> {
    const { slug, ...payload } = options;
    const { data } = await this.workos.put<
      DataIntegrationResponse,
      UpdateDataIntegrationResponse
    >(
      `/data-integrations/${encodeURIComponent(slug)}`,
      serializeUpdateDataIntegration(payload),
    );
    return deserializeDataIntegration(data);
  }

  /**
   * Delete a data integration
   *
   * Deletes the user-owned data integration and all of its connected installations. For a custom provider, the provider definition is deleted once no organization-owned root references it either.
   * @param options - The request options.
   * @param options.slug - The slug identifier of the data integration.
   * @example "github"
   * @returns {Promise<void>}
   * @throws {UnauthorizedException} 401
   * @throws {NotFoundException} 404
   */
  async deleteDataIntegration(
    options: DeleteDataIntegrationOptions,
  ): Promise<void> {
    const { slug } = options;
    await this.workos.delete(`/data-integrations/${encodeURIComponent(slug)}`);
  }

  /**
   * Upsert an API key for a connected account
   *
   * Creates or updates an API-key-based installation for the specified integration, owned by the user or, when `connection_owner` is `organization`, shared by the organization. If an installation already exists, the stored API key is rotated to the new value.
   * @param options - Object containing userId, secret.
   * @param options.slug - The identifier of the integration.
   * @example "github"
   * @param options.userId - A [User](https://workos.com/docs/reference/authkit/user) identifier.
   * @example "user_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.organizationId - An [Organization](https://workos.com/docs/reference/organization) identifier. Optional parameter to scope the connection to a specific organization. Required when `connection_owner` is `organization`.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.connectedAccountId - A [connected account](https://workos.com/docs/reference/pipes/connected-account) identifier. Use this to rotate a specific existing connection.
   * @example "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.connectionOwner - Whose connection to create or rotate. `user` (the default) addresses the connection owned by `user_id`. `organization` addresses the connection shared by every member of `organization_id`; `user_id` then identifies the member performing the request and must be an active member of the organization.
   * @example "user"
   * @param options.secret - The API key secret to store for this integration.
   * @example "sk-1234567890abcdef"
   * @returns {Promise<ConnectedAccount>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   */
  async updateDataIntegrationApiKey(
    options: UpdateDataIntegrationApiKeyOptions,
  ): Promise<ConnectedAccount> {
    const { slug, ...payload } = options;
    const { data } = await this.workos.put<
      ConnectedAccountResponse,
      DataIntegrationsUpsertApiKeyRequestResponse
    >(
      `/data-integrations/${encodeURIComponent(slug)}/api-key`,
      serializeDataIntegrationsUpsertApiKeyRequest(payload),
    );
    return deserializeConnectedAccount(data);
  }

  /**
   * Get authorization URL
   *
   * Generates an OAuth authorization URL to initiate the connection flow for a user. Redirect the user to the returned URL to begin the OAuth flow with the third-party provider.
   * @param options - Object containing userId.
   * @param options.slug - The slug identifier of the provider (e.g., `github`, `slack`, `notion`).
   * @example "github"
   * @param options.userId - The ID of the user to authorize. When `connection_owner` is `organization`, this is the user authorizing on behalf of the organization; they must be an active member of the organization and do not become the owner of the resulting connected account.
   * @example "user_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.organizationId - An organization ID to scope the authorization to a specific organization. Required when `connection_owner` is `organization`.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.connectionOwner - Who will own the connected account. `user` (the default) connects the user's own account. `organization` connects the organization's shared account and requires `organization_id`.
   * @example "user"
   * @param options.returnTo - The URL to redirect the user to after authorization.
   * @example "https://example.com/callback"
   * @param options.config - Connect-time config values for the provider-declared `installation`-scope fields (e.g. a Zendesk `subdomain`), keyed by the config field. Only fields the provider declares may be supplied, and required fields must be provided unless already pinned on the integration.
   * @example {"subdomain":"acme"}
   * @returns {Promise<DataIntegrationAuthorizeUrlResponse>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   */
  async authorizeDataIntegration(
    options: AuthorizeDataIntegrationOptions,
  ): Promise<DataIntegrationAuthorizeUrlResponse> {
    const { slug, ...payload } = options;
    const { data } = await this.workos.post<
      DataIntegrationAuthorizeUrlResponseWire,
      DataIntegrationsGetDataIntegrationAuthorizeUrlRequestResponse
    >(
      `/data-integrations/${encodeURIComponent(slug)}/authorize`,
      serializeDataIntegrationsGetDataIntegrationAuthorizeUrlRequest(payload),
    );
    return deserializeDataIntegrationAuthorizeUrlResponse(data);
  }

  /**
   * Upsert client credentials for a connected account
   *
   * Creates or updates a client-credentials-based installation for the specified integration, owned by the user or, when `connection_owner` is `organization`, shared by the organization. If an installation already exists, the stored client credentials are rotated to the new values.
   * @param options - The request options.
   * @param options.slug - The identifier of the integration.
   * @example "salesforce"
   * @returns {Promise<ConnectedAccount>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   */
  async updateDataIntegrationClientCredentials(
    options: UpdateDataIntegrationClientCredentialsOptions,
  ): Promise<ConnectedAccount> {
    const { slug, ...payload } = options;
    const { data } = await this.workos.put<
      ConnectedAccountResponse,
      DataIntegrationsUpsertClientCredentialsRequestResponse
    >(
      `/data-integrations/${encodeURIComponent(slug)}/client-credentials`,
      serializeDataIntegrationsUpsertClientCredentialsRequest(payload),
    );
    return deserializeConnectedAccount(data);
  }

  /**
   * Vend credentials for a connected account
   *
   * Returns credentials for a user-owned or organization-owned connected account. OAuth and client-credentials installations return an access token; API-key installations return the stored secret. Client-credentials responses also include provider metadata.
   * @param options - Object containing userId.
   * @param options.slug - The identifier of the integration.
   * @example "github"
   * @param options.userId - A [User](https://workos.com/docs/reference/authkit/user) identifier. When `connection_owner` is `organization`, this is the user the credentials are vended on behalf of; they must be an active member of the organization.
   * @example "user_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.organizationId - An [Organization](https://workos.com/docs/reference/organization) identifier. Optional parameter to scope the connection to a specific organization. Required when `connection_owner` is `organization`.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.connectedAccountId - A [connected account](https://workos.com/docs/reference/pipes/connected-account) identifier. Use this to select a specific connection when the user has several for this provider.
   * @example "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.connectionOwner - Which connection to vend from. `user` (the default) vends the user's own connection and requires `user_id`. `organization` vends the organization's shared connection and requires `organization_id`.
   * @example "user"
   * @param options.supportsMultipleConnections - Set to `true` to use the plural connection contract. If no `connected_account_id` is supplied and several connections match, the request returns `account_selection_required`. When omitted or `false`, only the compatibility connection is considered.
   * @example true
   * @returns {Promise<DataIntegrationCredentialsResponse>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   */
  async createDataIntegrationCredential(
    options: CreateDataIntegrationCredentialOptions,
  ): Promise<DataIntegrationCredentialsResponse> {
    const { slug, ...payload } = options;
    const { data } = await this.workos.post<
      DataIntegrationCredentialsResponseWire,
      DataIntegrationsVendCredentialsRequestResponse
    >(
      `/data-integrations/${encodeURIComponent(slug)}/credentials`,
      serializeDataIntegrationsVendCredentialsRequest(payload),
    );
    return deserializeDataIntegrationCredentialsResponse(data);
  }

  /**
   * Get an organization-owned data integration
   *
   * Retrieves the organization-owned data integration for a provider by its slug. The `/organization` suffix selects the environment-level organization-owned root for the provider; it does not name a particular organization.
   * @param options - The request options.
   * @param options.slug - The slug identifier of the data integration.
   * @example "github"
   * @returns {Promise<DataIntegration>}
   * @throws {UnauthorizedException} 401
   * @throws {NotFoundException} 404
   */
  async getOrganizationDataIntegration(
    options: GetOrganizationDataIntegrationOptions,
  ): Promise<DataIntegration> {
    const { slug } = options;
    const { data } = await this.workos.get<DataIntegrationResponse>(
      `/data-integrations/${encodeURIComponent(slug)}/organization`,
    );
    return deserializeDataIntegration(data);
  }

  /**
   * Update an organization-owned data integration
   *
   * Updates the description, enabled state, or custom credentials of the organization-owned data integration for a provider. For custom providers, `custom_provider` updates the OAuth definition, which is shared with the user-owned root. The `/organization` suffix selects the environment-level organization-owned root for the provider; it does not name a particular organization.
   * @param options - The request options.
   * @param options.slug - The slug identifier of the data integration.
   * @example "github"
   * @returns {Promise<DataIntegration>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   */
  async updateOrganizationDataIntegration(
    options: UpdateOrganizationDataIntegrationOptions,
  ): Promise<DataIntegration> {
    const { slug, ...payload } = options;
    const { data } = await this.workos.put<
      DataIntegrationResponse,
      UpdateDataIntegrationResponse
    >(
      `/data-integrations/${encodeURIComponent(slug)}/organization`,
      serializeUpdateDataIntegration(payload),
    );
    return deserializeDataIntegration(data);
  }

  /**
   * Delete an organization-owned data integration
   *
   * Deletes the organization-owned data integration for a provider and all of its connected installations. For a custom provider, the provider definition is deleted once no user-owned root references it either. The `/organization` suffix selects the environment-level organization-owned root for the provider; it does not name a particular organization.
   * @param options - The request options.
   * @param options.slug - The slug identifier of the data integration.
   * @example "github"
   * @returns {Promise<void>}
   * @throws {UnauthorizedException} 401
   * @throws {NotFoundException} 404
   */
  async deleteOrganizationDataIntegration(
    options: DeleteOrganizationDataIntegrationOptions,
  ): Promise<void> {
    const { slug } = options;
    await this.workos.delete(
      `/data-integrations/${encodeURIComponent(slug)}/organization`,
    );
  }

  /**
   * Get an access token for a connected account
   *
   * Fetches a valid OAuth access token for a user's connected account. WorkOS automatically handles token refresh, ensuring you always receive a valid, non-expired token.
   * @param options - Object containing userId.
   * @param options.provider - The identifier of the integration.
   * @example "github"
   * @param options.userId - A [User](https://workos.com/docs/reference/authkit/user) identifier. When `connection_owner` is `organization`, this is the user the credentials are vended on behalf of; they must be an active member of the organization.
   * @example "user_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.organizationId - An [Organization](https://workos.com/docs/reference/organization) identifier. Optional parameter to scope the connection to a specific organization. Required when `connection_owner` is `organization`.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.connectedAccountId - A [connected account](https://workos.com/docs/reference/pipes/connected-account) identifier. Use this to select a specific connection when the user has several for this provider.
   * @example "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.connectionOwner - Which connection to vend from. `user` (the default) vends the user's own connection and requires `user_id`. `organization` vends the organization's shared connection and requires `organization_id`.
   * @example "user"
   * @param options.supportsMultipleConnections - Set to `true` to use the plural connection contract. If no `connected_account_id` is supplied and several connections match, the request returns `account_selection_required`. When omitted or `false`, only the compatibility connection is considered.
   * @example true
   * @returns {Promise<DataIntegrationAccessTokenResponse>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   */
  async getAccessToken(
    options: GetAccessTokenOptions,
  ): Promise<DataIntegrationAccessTokenResponse> {
    const { provider, ...payload } = options;
    const { data } = await this.workos.post<
      DataIntegrationAccessTokenResponseWire,
      DataIntegrationsGetUserTokenRequestResponse
    >(
      `/data-integrations/${encodeURIComponent(provider)}/token`,
      serializeDataIntegrationsGetUserTokenRequest(payload),
    );
    return deserializeDataIntegrationAccessTokenResponse(data);
  }

  /**
   * Get an organization connected account
   *
   * Retrieves an organization's [connected account](https://workos.com/docs/reference/pipes/connected-account) for a specific provider.
   * @param options - Additional query options.
   * @param options.organizationId - An [Organization](https://workos.com/docs/reference/organization) identifier.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.slug - The slug identifier of the provider (e.g., `github`, `slack`, `notion`).
   * @example "github"
   * @param options.supportsMultipleConnections - Set to `true` to use the plural connection contract. When omitted or `false`, only the compatibility connection is considered.
   * @example true
   * @param options.connectedAccountId - A [connected account](https://workos.com/docs/reference/pipes/connected-account) identifier. Use this to select a specific connection when the organization has several for this provider.
   * @example "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
   * @returns {Promise<ConnectedAccount>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   */
  async getOrganizationConnectedAccount(
    options: GetOrganizationConnectedAccountOptions,
  ): Promise<ConnectedAccount> {
    const { organizationId, slug } = options;
    const { data } = await this.workos.get<ConnectedAccountResponse>(
      `/organizations/${encodeURIComponent(
        organizationId,
      )}/connected_accounts/${encodeURIComponent(slug)}`,
      {
        query: {
          ...(options.supportsMultipleConnections !== undefined && {
            supports_multiple_connections: options.supportsMultipleConnections,
          }),
          ...(options.connectedAccountId !== undefined && {
            connected_account_id: options.connectedAccountId,
          }),
        },
      },
    );
    return deserializeConnectedAccount(data);
  }

  /**
   * Import an organization connected account
   *
   * Imports an organization-owned [connected account](https://workos.com/docs/reference/pipes/connected-account) by providing OAuth tokens directly. Use this to migrate existing connections or set up connections without going through the OAuth flow.
   * @param options - The request options.
   * @param options.organizationId - An [Organization](https://workos.com/docs/reference/organization) identifier.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.slug - The slug identifier of the provider (e.g., `github`, `slack`, `notion`).
   * @example "github"
   * @returns {Promise<ConnectedAccount>}
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   */
  async createOrganizationConnectedAccount(
    options: CreateOrganizationConnectedAccountOptions,
  ): Promise<ConnectedAccount> {
    const { organizationId, slug, ...payload } = options;
    const { data } = await this.workos.post<
      ConnectedAccountResponse,
      OrganizationConnectedAccountResponse
    >(
      `/organizations/${encodeURIComponent(
        organizationId,
      )}/connected_accounts/${encodeURIComponent(slug)}`,
      serializeOrganizationConnectedAccount(payload),
    );
    return deserializeConnectedAccount(data);
  }

  /**
   * Update an organization connected account
   *
   * Updates an organization's [connected account](https://workos.com/docs/reference/pipes/connected-account) tokens, scopes, or state for a specific provider.
   * @param options - Additional query options.
   * @param options.organizationId - An [Organization](https://workos.com/docs/reference/organization) identifier.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.slug - The slug identifier of the provider (e.g., `github`, `slack`, `notion`).
   * @example "github"
   * @param options.supportsMultipleConnections - Set to `true` to use the plural connection contract. When omitted or `false`, only the compatibility connection is considered.
   * @example true
   * @param options.connectedAccountId - A [connected account](https://workos.com/docs/reference/pipes/connected-account) identifier. Use this to select the connection to update.
   * @example "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
   * @returns {Promise<ConnectedAccount>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   */
  async updateOrganizationConnectedAccount(
    options: UpdateOrganizationConnectedAccountOptions,
  ): Promise<ConnectedAccount> {
    const {
      organizationId,
      slug,
      supportsMultipleConnections,
      connectedAccountId,
      ...payload
    } = options;
    const { data } = await this.workos.put<
      ConnectedAccountResponse,
      OrganizationConnectedAccountResponse
    >(
      `/organizations/${encodeURIComponent(
        organizationId,
      )}/connected_accounts/${encodeURIComponent(slug)}`,
      serializeOrganizationConnectedAccount(payload),
      {
        query: {
          supports_multiple_connections: supportsMultipleConnections,
          connected_account_id: connectedAccountId,
        },
      },
    );
    return deserializeConnectedAccount(data);
  }

  /**
   * Delete an organization connected account
   *
   * Disconnects the organization's account for the provider, including removing any stored access and refresh tokens. A member will need to reauthorize if the organization wants to reconnect. This does not revoke access on the provider side.
   * @param options - Additional query options.
   * @param options.organizationId - An [Organization](https://workos.com/docs/reference/organization) identifier.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.slug - The slug identifier of the provider (e.g., `github`, `slack`, `notion`).
   * @example "github"
   * @param options.supportsMultipleConnections - Set to `true` to use the plural connection contract. When omitted or `false`, only the compatibility connection is considered.
   * @example true
   * @param options.connectedAccountId - A [connected account](https://workos.com/docs/reference/pipes/connected-account) identifier. Use this to select the connection to delete.
   * @example "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   */
  async deleteOrganizationConnectedAccount(
    options: DeleteOrganizationConnectedAccountOptions,
  ): Promise<void> {
    const { organizationId, slug } = options;
    await this.workos.delete(
      `/organizations/${encodeURIComponent(organizationId)}/connected_accounts/${encodeURIComponent(slug)}`,
      {
        supports_multiple_connections: options.supportsMultipleConnections,
        connected_account_id: options.connectedAccountId,
      },
    );
  }

  /**
   * List providers for an organization
   *
   * Retrieves the organization-owned providers configured for your environment and the organization's [connected account](https://workos.com/docs/reference/pipes/connected-account) information for each. Providers owned by individual users are not included.
   * @param options - Additional query options.
   * @param options.organizationId - An [Organization](https://workos.com/docs/reference/organization) identifier to list providers and connected accounts for.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.supportsMultipleConnections - Set to `true` to use the plural connection contract. When omitted or `false`, only the compatibility connection is considered.
   * @example true
   * @returns {Promise<DataIntegrationsListResponse>}
   * @throws {UnauthorizedException} 401
   * @throws {NotFoundException} 404
   */
  async listOrganizationDataProviders(
    options: ListOrganizationDataProvidersOptions,
  ): Promise<DataIntegrationsListResponse> {
    const { organizationId } = options;
    const { data } = await this.workos.get<DataIntegrationsListResponseWire>(
      `/organizations/${encodeURIComponent(organizationId)}/data_providers`,
      {
        query: {
          ...(options.supportsMultipleConnections !== undefined && {
            supports_multiple_connections: options.supportsMultipleConnections,
          }),
        },
      },
    );
    return deserializeDataIntegrationsListResponse(data);
  }

  /**
   * Get a connected account
   *
   * Retrieves a user's [connected account](https://workos.com/docs/reference/pipes/connected-account) for a specific provider.
   * @param options - Additional query options.
   * @param options.userId - A [User](https://workos.com/docs/reference/authkit/user) identifier.
   * @example "user_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.slug - The slug identifier of the provider (e.g., `github`, `slack`, `notion`).
   * @example "github"
   * @param options.organizationId - An [Organization](https://workos.com/docs/reference/organization) identifier. Optional parameter if the connection is scoped to an organization.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.supportsMultipleConnections - Set to `true` to use the plural connection contract. When omitted or `false`, only the compatibility connection is considered.
   * @example true
   * @param options.connectedAccountId - A [connected account](https://workos.com/docs/reference/pipes/connected-account) identifier. Use this to select a specific connection when the user has several for this provider.
   * @example "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
   * @returns {Promise<ConnectedAccount>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   */
  async getUserConnectedAccount(
    options: GetUserConnectedAccountOptions,
  ): Promise<ConnectedAccount> {
    const { userId, slug } = options;
    const { data } = await this.workos.get<ConnectedAccountResponse>(
      `/user_management/users/${encodeURIComponent(
        userId,
      )}/connected_accounts/${encodeURIComponent(slug)}`,
      {
        query: {
          ...(options.organizationId !== undefined && {
            organization_id: options.organizationId,
          }),
          ...(options.supportsMultipleConnections !== undefined && {
            supports_multiple_connections: options.supportsMultipleConnections,
          }),
          ...(options.connectedAccountId !== undefined && {
            connected_account_id: options.connectedAccountId,
          }),
        },
      },
    );
    return deserializeConnectedAccount(data);
  }

  /**
   * Import a connected account
   *
   * Imports a [connected account](https://workos.com/docs/reference/pipes/connected-account) for a user by providing OAuth tokens directly. Use this to migrate existing connections or set up connections without going through the OAuth flow.
   * @param options - The request body.
   * @param options.userId - A [User](https://workos.com/docs/reference/authkit/user) identifier.
   * @example "user_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.slug - The slug identifier of the provider (e.g., `github`, `slack`, `notion`).
   * @example "github"
   * @param options.organizationId - An [Organization](https://workos.com/docs/reference/organization) identifier. Optional parameter if the connection is scoped to an organization.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.accessToken - The OAuth access token for the connected account.
   * @example "gho_16C7e42F292c6912E7710c838347Ae178B4a"
   * @param options.refreshToken - The OAuth refresh token for the connected account.
   * @example "ghr_xxxxxxxxxxxxxxxxxxxx"
   * @param options.expiresAt - The ISO-8601 timestamp when the access token expires. Required when `access_token` is provided for tokens that expire.
   * @example "2025-12-31T23:59:59.000Z"
   * @param options.scopes - The OAuth scopes granted for this connection.
   * @example ["repo","user:email"]
   * @param options.state - Explicitly set the state of the connected account. When omitted, the state is derived from the token combination provided.
   * @example "connected"
   * @returns {Promise<ConnectedAccount>}
   * @throws {UnauthorizedException} 401
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   */
  async createUserConnectedAccount(
    options: CreateUserConnectedAccountOptions,
  ): Promise<ConnectedAccount> {
    const { userId, slug, organizationId, ...payload } = options;
    const { data } = await this.workos.post<
      ConnectedAccountResponse,
      ConnectedAccountInputResponse
    >(
      `/user_management/users/${encodeURIComponent(
        userId,
      )}/connected_accounts/${encodeURIComponent(slug)}`,
      serializeConnectedAccountInput(payload),
      { query: { organization_id: organizationId } },
    );
    return deserializeConnectedAccount(data);
  }

  /**
   * Update a connected account
   *
   * Updates a user's [connected account](https://workos.com/docs/reference/pipes/connected-account) tokens, scopes, or state for a specific provider.
   * @param options - The request body.
   * @param options.userId - A [User](https://workos.com/docs/reference/authkit/user) identifier.
   * @example "user_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.slug - The slug identifier of the provider (e.g., `github`, `slack`, `notion`).
   * @example "github"
   * @param options.organizationId - An [Organization](https://workos.com/docs/reference/organization) identifier. Optional parameter if the connection is scoped to an organization.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.supportsMultipleConnections - Set to `true` to use the plural connection contract. When omitted or `false`, only the compatibility connection is considered.
   * @example true
   * @param options.connectedAccountId - A [connected account](https://workos.com/docs/reference/pipes/connected-account) identifier. Use this to select the connection to update.
   * @example "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.accessToken - The OAuth access token for the connected account.
   * @example "gho_16C7e42F292c6912E7710c838347Ae178B4a"
   * @param options.refreshToken - The OAuth refresh token for the connected account.
   * @example "ghr_xxxxxxxxxxxxxxxxxxxx"
   * @param options.expiresAt - The ISO-8601 timestamp when the access token expires. Required when `access_token` is provided for tokens that expire.
   * @example "2025-12-31T23:59:59.000Z"
   * @param options.scopes - The OAuth scopes granted for this connection.
   * @example ["repo","user:email"]
   * @param options.state - Explicitly set the state of the connected account. When omitted, the state is derived from the token combination provided.
   * @example "connected"
   * @returns {Promise<ConnectedAccount>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   */
  async updateUserConnectedAccount(
    options: UpdateUserConnectedAccountOptions,
  ): Promise<ConnectedAccount> {
    const {
      userId,
      slug,
      organizationId,
      supportsMultipleConnections,
      connectedAccountId,
      ...payload
    } = options;
    const { data } = await this.workos.put<
      ConnectedAccountResponse,
      ConnectedAccountInputResponse
    >(
      `/user_management/users/${encodeURIComponent(
        userId,
      )}/connected_accounts/${encodeURIComponent(slug)}`,
      serializeConnectedAccountInput(payload),
      {
        query: {
          organization_id: organizationId,
          supports_multiple_connections: supportsMultipleConnections,
          connected_account_id: connectedAccountId,
        },
      },
    );
    return deserializeConnectedAccount(data);
  }

  /**
   * Delete a connected account
   *
   * Disconnects WorkOS's account for the user, including removing any stored access and refresh tokens. The user will need to reauthorize if they want to reconnect. Access is not revoked on the provider side, except for the WorkOS OAuth provider, whose underlying AuthKit grant is revoked.
   * @param options - Additional query options.
   * @param options.userId - A [User](https://workos.com/docs/reference/authkit/user) identifier.
   * @example "user_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.slug - The slug identifier of the provider (e.g., `github`, `slack`, `notion`).
   * @example "github"
   * @param options.organizationId - An [Organization](https://workos.com/docs/reference/organization) identifier. Optional parameter if the connection is scoped to an organization.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.supportsMultipleConnections - Set to `true` to use the plural connection contract. When omitted or `false`, only the compatibility connection is considered.
   * @example true
   * @param options.connectedAccountId - A [connected account](https://workos.com/docs/reference/pipes/connected-account) identifier. Use this to select the connection to delete.
   * @example "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
   * @returns {Promise<void>}
   * @throws {BadRequestException} 400
   * @throws {UnauthorizedException} 401
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   */
  async deleteUserConnectedAccount(
    options: DeleteUserConnectedAccountOptions,
  ): Promise<void> {
    const { userId, slug } = options;
    await this.workos.delete(
      `/user_management/users/${encodeURIComponent(userId)}/connected_accounts/${encodeURIComponent(slug)}`,
      {
        organization_id: options.organizationId,
        supports_multiple_connections: options.supportsMultipleConnections,
        connected_account_id: options.connectedAccountId,
      },
    );
  }

  /**
   * List providers for a user
   *
   * Retrieves a list of available providers and the user's connection status for each. Returns all providers configured for your environment, along with the user's [connected account](https://workos.com/docs/reference/pipes/connected-account) information where applicable.
   * @param options - Additional query options.
   * @param options.userId - A [User](https://workos.com/docs/reference/authkit/user) identifier to list providers and connected accounts for.
   * @example "user_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.organizationId - An [Organization](https://workos.com/docs/reference/organization) identifier. Optional parameter to filter connections for a specific organization.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.supportsMultipleConnections - Set to `true` to use the plural connection contract. When omitted or `false`, only the compatibility connection is considered.
   * @example true
   * @returns {Promise<DataIntegrationsListResponse>}
   * @throws {UnauthorizedException} 401
   * @throws {NotFoundException} 404
   */
  async listUserDataProviders(
    options: ListUserDataProvidersOptions,
  ): Promise<DataIntegrationsListResponse> {
    const { userId } = options;
    const { data } = await this.workos.get<DataIntegrationsListResponseWire>(
      `/user_management/users/${encodeURIComponent(userId)}/data_providers`,
      {
        query: {
          ...(options.organizationId !== undefined && {
            organization_id: options.organizationId,
          }),
          ...(options.supportsMultipleConnections !== undefined && {
            supports_multiple_connections: options.supportsMultipleConnections,
          }),
        },
      },
    );
    return deserializeDataIntegrationsListResponse(data);
  }
}
