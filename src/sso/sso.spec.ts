// This file is auto-generated by oagen. Do not edit.

import fetch from 'jest-fetch-mock';
import {
  fetchOnce,
  fetchURL,
  fetchMethod,
  fetchSearchParams,
  fetchBody,
} from '../common/utils/test-utils';
import { WorkOS } from '../workos';
import { ConnectionType } from './interfaces/connection-type.enum';

import listConnectionFixture from './fixtures/list-connection.json';
import ssoLogoutAuthorizeResponseFixture from './fixtures/sso-logout-authorize-response.json';

const workos = new WorkOS('sk_test_Sz3IQjepeSWaI4cMS4ms4sMuU');

function expectConnection(result: any) {
  expect(result.object).toBe('connection');
  expect(result.id).toBe('conn_01E4ZCR3C56J083X43JQXF3JK5');
  expect(result.connectionType).toBe('OktaSAML');
  expect(result.name).toBe('Foo Corp');
  expect(result.state).toBe('active');
  expect(result.status).toBe('linked');
  expect(result.createdAt.toISOString()).toBe('2026-01-15T12:00:00.000Z');
  expect(result.updatedAt.toISOString()).toBe('2026-01-15T12:00:00.000Z');
}

describe('SSO', () => {
  beforeEach(() => fetch.resetMocks());

  describe('listConnections', () => {
    it('returns paginated results', async () => {
      fetchOnce(listConnectionFixture);

      const { data, listMetadata } = await workos.sso.listConnections({
        order: 'desc',
        connectionType: ConnectionType.ADFSSAML,
        domain: 'foo-corp.com',
        organizationId: 'org_01EHWNCE74X7JSDV0X3SZ3KJNY',
        search: 'Foo Corp',
      });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe('/connections');
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
      expectConnection(data[0]);
    });
  });

  describe('authorizeLogout', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(ssoLogoutAuthorizeResponseFixture);

      const result = await workos.sso.authorizeLogout({
        profileId: 'profile_id_01234',
      });

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/sso/logout/authorize',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({ profile_id: 'profile_id_01234' }),
      );
      expect(result.logoutUrl).toBe(
        'https://auth.workos.com/sso/logout?token=eyJhbGciOiJSUzI1NiJ9',
      );
      expect(result.logoutToken).toBe(
        'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJwcm9maWxlX2lkIjoicHJvZl8wMUdXUTFHMEgyRk02QVNFRjBIUzEzSENXOS0zMDRrZzAzZyIsImV4cCI6IjE1MTYyMzkwMjIifQ.Wru9Qlnf5DpohtGCKhZU4cVOd3zpiu7QQ-XEX--5A_4',
      );
    });
  });
});
