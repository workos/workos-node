// This file is auto-generated by oagen. Do not edit.

import type { Profile, ProfileResponse } from './profile.interface';
import type {
  SSOTokenResponseOAuthToken,
  SSOTokenResponseOAuthTokenResponse,
} from './sso-token-response-oauth-token.interface';

export interface SSOTokenResponse {
  /** The type of token issued. */
  tokenType: 'Bearer';
  /** An access token that can be exchanged for a user profile. Access tokens are short-lived — see the `expires_in` field for the exact lifetime. */
  accessToken: string;
  /** The lifetime of the access token in seconds. */
  expiresIn: number;
  profile: Profile<Record<string, unknown>>;
  /** OAuth tokens issued by the identity provider, if available. */
  oauthTokens?: SSOTokenResponseOAuthToken;
}

export interface SSOTokenResponseWire {
  token_type: 'Bearer';
  access_token: string;
  expires_in: number;
  profile: ProfileResponse<Record<string, unknown>>;
  oauth_tokens?: SSOTokenResponseOAuthTokenResponse;
}
