// This file is auto-generated by oagen. Do not edit.

import type { ConnectionDomain } from './connection-domain.interface';
import type {
  ConnectionOption,
  ConnectionOptionResponse,
} from './connection-option.interface';
import type { ConnectionType } from './connection-type.interface';
import type { ConnectionStatus } from './connection-status.interface';

export interface Connection {
  /** Distinguishes the Connection object. */
  object: 'connection';
  /** Unique identifier for the Connection. */
  id: string;
  /** Unique identifier for the Organization in which the Connection resides. */
  organizationId?: string;
  /** The type of the SSO Connection used to authenticate the user. The Connection type may be used to dynamically generate authorization URLs. */
  connectionType?: ConnectionType;
  /** A human-readable name for the Connection. This will most commonly be the organization's name. */
  name: string;
  /** Indicates whether a Connection is able to authenticate users. */
  state: 'active' | 'inactive' | 'validating' | 'draft';
  /**
   * Deprecated. Use `state` instead.
   * @deprecated
   */
  status?: ConnectionStatus;
  /** List of Organization Domains. */
  domains: ConnectionDomain[];
  /** Configuration options for SAML connections. Only present for SAML connection types. */
  options?: ConnectionOption;
  /** An ISO 8601 timestamp. */
  createdAt: Date;
  /** An ISO 8601 timestamp. */
  updatedAt: Date;
}

export interface ConnectionResponse {
  object: 'connection';
  id: string;
  organization_id?: string;
  connection_type: ConnectionType;
  name: string;
  state: 'active' | 'inactive' | 'validating' | 'draft';
  status?: ConnectionStatus;
  domains: ConnectionDomain[];
  options?: ConnectionOptionResponse;
  created_at: string;
  updated_at: string;
}
