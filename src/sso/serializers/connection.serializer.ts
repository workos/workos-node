// This file is auto-generated by oagen. Do not edit.

import type {
  Connection,
  ConnectionResponse,
} from '../interfaces/connection.interface';
import { deserializeConnectionDomain } from './connection-domain.serializer';
import { deserializeConnectionOption } from './connection-option.serializer';

export const deserializeConnection = (
  response: ConnectionResponse,
): Connection => ({
  object: response.object,
  id: response.id,
  organizationId: response.organization_id,
  type: response.connection_type,
  name: response.name,
  state: response.state,
  status: response.status,
  domains: response.domains.map(deserializeConnectionDomain),
  options:
    response.options != null
      ? deserializeConnectionOption(response.options)
      : undefined,
  createdAt: new Date(response.created_at),
  updatedAt: new Date(response.updated_at),
});
