// This file is auto-generated by oagen. Do not edit.

import type {
  SessionCreatedDataImpersonator,
  SessionCreatedDataImpersonatorResponse,
} from './session-created-data-impersonator.interface';
import type { SessionCreatedDataAuthMethod } from './session-created-data-auth-method.interface';
import type { SessionCreatedDataStatus } from './session-created-data-status.interface';

/** The event payload. */
export interface SessionCreatedData {
  /** Distinguishes the session object. */
  object: 'session';
  /** The unique ID of the session. */
  id: string;
  /** Information about the impersonator if this session was created via impersonation. */
  impersonator?: SessionCreatedDataImpersonator;
  /** The IP address from which the session was created. */
  ipAddress: string | null;
  /** The ID of the organization this session is associated with. */
  organizationId?: string;
  /** The user agent string from the device that created the session. */
  userAgent: string | null;
  /** The ID of the user this session belongs to. */
  userId: string;
  /** The authentication method used to create this session. */
  authMethod: SessionCreatedDataAuthMethod;
  /** The current status of the session. */
  status: SessionCreatedDataStatus;
  /** The timestamp when the session expires. */
  expiresAt: string;
  /** The timestamp when the session ended. */
  endedAt: string | null;
  /** An ISO 8601 timestamp. */
  createdAt: string;
  /** An ISO 8601 timestamp. */
  updatedAt: string;
}

export interface SessionCreatedDataResponse {
  object: 'session';
  id: string;
  impersonator?: SessionCreatedDataImpersonatorResponse;
  ip_address: string | null;
  organization_id?: string;
  user_agent: string | null;
  user_id: string;
  auth_method: SessionCreatedDataAuthMethod;
  status: SessionCreatedDataStatus;
  expires_at: string;
  ended_at: string | null;
  created_at: string;
  updated_at: string;
}
