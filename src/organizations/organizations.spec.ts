// This file is auto-generated by oagen. Do not edit.

import fetch from 'jest-fetch-mock';
import {
  fetchOnce,
  fetchURL,
  fetchMethod,
  fetchSearchParams,
  fetchBody,
} from '../common/utils/test-utils';
import { WorkOS } from '../workos';

import listOrganizationFixture from './fixtures/list-organization.json';
import organizationFixture from './fixtures/organization.json';
import auditLogConfigurationFixture from './fixtures/audit-log-configuration.json';

const workos = new WorkOS('sk_test_Sz3IQjepeSWaI4cMS4ms4sMuU');

function expectOrganization(result: any) {
  expect(result.object).toBe('organization');
  expect(result.id).toBe('org_01EHWNCE74X7JSDV0X3SZ3KJNY');
  expect(result.name).toBe('Acme Inc.');
  expect(result.metadata).toEqual({ tier: 'diamond' });
  expect(result.externalId).toBe('2fe01467-f7ea-4dd2-8b79-c2b4f56d0191');
  expect(result.createdAt.toISOString()).toBe('2026-01-15T12:00:00.000Z');
  expect(result.updatedAt.toISOString()).toBe('2026-01-15T12:00:00.000Z');
}

describe('Organizations', () => {
  beforeEach(() => fetch.resetMocks());

  describe('listOrganizations', () => {
    it('returns paginated results', async () => {
      fetchOnce(listOrganizationFixture);

      const { data, listMetadata } =
        await workos.organizations.listOrganizations({
          order: 'desc',
          domains: ['foo-corp.com'],
          search: 'Acme Corp',
        });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe('/organizations');
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
      expectOrganization(data[0]);
    });
  });

  describe('createOrganization', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(organizationFixture);

      const result = await workos.organizations.createOrganization({
        name: 'Test',
      });

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe('/organizations');
      expect(fetchBody()).toEqual(expect.objectContaining({ name: 'Test' }));
      expectOrganization(result);
    });
  });

  describe('getOrganizationByExternalId', () => {
    it('returns the expected result', async () => {
      fetchOnce(organizationFixture);

      const result = await workos.organizations.getOrganizationByExternalId({
        externalId: 'test_externalId',
      });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/organizations/external_id/test_externalId',
      );
      expectOrganization(result);
    });
  });

  describe('getOrganization', () => {
    it('returns the expected result', async () => {
      fetchOnce(organizationFixture);

      const result = await workos.organizations.getOrganization({
        id: 'test_id',
      });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/organizations/test_id',
      );
      expectOrganization(result);
    });
  });

  describe('updateOrganization', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(organizationFixture);

      const result = await workos.organizations.updateOrganization({
        id: 'test_id',
        name: 'Test',
        allowProfilesOutsideOrganization: true,
      });

      expect(fetchMethod()).toBe('PUT');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/organizations/test_id',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({
          name: 'Test',
          allow_profiles_outside_organization: true,
        }),
      );
      expectOrganization(result);
    });
  });

  describe('deleteOrganization', () => {
    it('sends a DELETE request', async () => {
      fetchOnce({}, { status: 204 });

      await workos.organizations.deleteOrganization({ id: 'test_id' });

      expect(fetchMethod()).toBe('DELETE');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/organizations/test_id',
      );
    });
  });

  describe('getAuditLogConfiguration', () => {
    it('returns the expected result', async () => {
      fetchOnce(auditLogConfigurationFixture);

      const result = await workos.organizations.getAuditLogConfiguration({
        id: 'test_id',
      });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/organizations/test_id/audit_log_configuration',
      );
      expect(result.organizationId).toBe('org_01EHZNVPK3SFK441A1RGBFSHRT');
      expect(result.retentionPeriodInDays).toBe(30);
      expect(result.state).toBe('active');
    });
  });
});
