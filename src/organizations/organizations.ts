// This file is auto-generated by oagen. Do not edit.

import type { WorkOS } from '../workos';
import { AutoPaginatable } from '../common/utils/pagination';
import { fetchAndDeserialize } from '../common/utils/fetch-and-deserialize';
import type { ListOrganizationsOptions } from './interfaces/list-organizations-options.interface';
import type { CreateOrganizationOptions } from './interfaces/create-organization-options.interface';
import type { GetOrganizationByExternalIdOptions } from './interfaces/get-organization-by-external-id-options.interface';
import type { GetOrganizationOptions } from './interfaces/get-organization-options.interface';
import type { UpdateOrganizationOptions } from './interfaces/update-organization-options.interface';
import type { DeleteOrganizationOptions } from './interfaces/delete-organization-options.interface';
import type { GetAuditLogConfigurationOptions } from './interfaces/get-audit-log-configuration-options.interface';
import type {
  Organization,
  OrganizationResponse,
} from './interfaces/organization.interface';
import type {
  AuditLogConfiguration,
  AuditLogConfigurationResponse,
} from './interfaces/audit-log-configuration.interface';
import type { OrganizationInputResponse } from './interfaces/organization-input.interface';
import type { UpdateOrganizationResponse } from './interfaces/update-organization.interface';
import { deserializeOrganization } from './serializers/organization.serializer';
import { deserializeAuditLogConfiguration } from './serializers/audit-log-configuration.serializer';
import { serializeOrganizationInput } from './serializers/organization-input.serializer';
import { serializeUpdateOrganization } from './serializers/update-organization.serializer';

export class Organizations {
  constructor(private readonly workos: WorkOS) {}

  /**
   * List Organizations
   *
   * Get a list of all of your existing organizations matching the criteria specified.
   * @param options - Pagination and filter options.
   * @returns {Promise<AutoPaginatable<Organization, ListOrganizationsOptions>>}
   * @throws {UnprocessableEntityException} 422
   */
  async listOrganizations(
    options?: ListOrganizationsOptions,
  ): Promise<AutoPaginatable<Organization, ListOrganizationsOptions>> {
    const paginationOptions = options;
    return new AutoPaginatable(
      await fetchAndDeserialize<OrganizationResponse, Organization>(
        this.workos,
        '/organizations',
        deserializeOrganization,
        paginationOptions,
      ),
      (params) =>
        fetchAndDeserialize<OrganizationResponse, Organization>(
          this.workos,
          '/organizations',
          deserializeOrganization,
          params,
        ),
      paginationOptions,
    );
  }

  /**
   * Create an Organization
   *
   * Creates a new organization in the current environment.
   * @param options - The request options.
   * @returns {Promise<Organization>}
   * @throws {BadRequestException} 400
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   */
  async createOrganization(
    options: CreateOrganizationOptions,
  ): Promise<Organization> {
    const payload = options;
    const { data } = await this.workos.post<
      OrganizationResponse,
      OrganizationInputResponse
    >('/organizations', serializeOrganizationInput(payload));
    return deserializeOrganization(data);
  }

  /**
   * Get an Organization by External ID
   *
   * Get the details of an existing organization by an [external identifier](https://workos.com/docs/authkit/metadata/external-identifiers).
   * @param options - The request options.
   * @param options.externalId - The external ID of the Organization.
   * @example "2fe01467-f7ea-4dd2-8b79-c2b4f56d0191"
   * @returns {Promise<Organization>}
   * @throws {NotFoundException} 404
   */
  async getOrganizationByExternalId(
    options: GetOrganizationByExternalIdOptions,
  ): Promise<Organization> {
    const { externalId } = options;
    const { data } = await this.workos.get<OrganizationResponse>(
      `/organizations/external_id/${encodeURIComponent(externalId)}`,
    );
    return deserializeOrganization(data);
  }

  /**
   * Get an Organization
   *
   * Get the details of an existing organization.
   * @param options - The request options.
   * @param options.id - Unique identifier of the Organization.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @returns {Promise<Organization>}
   * @throws {NotFoundException} 404
   */
  async getOrganization(
    options: GetOrganizationOptions,
  ): Promise<Organization> {
    const { id } = options;
    const { data } = await this.workos.get<OrganizationResponse>(
      `/organizations/${encodeURIComponent(id)}`,
    );
    return deserializeOrganization(data);
  }

  /**
   * Update an Organization
   *
   * Updates an organization in the current environment.
   * @param options - The request body.
   * @param options.id - Unique identifier of the Organization.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options.name - The name of the organization.
   * @example "Foo Corp"
   * @param options.allowProfilesOutsideOrganization - Whether the organization allows profiles from outside the organization to sign in.
   * @example false
   * @param options.domains - (deprecated) The domains associated with the organization. Deprecated in favor of `domain_data`.
   * @example ["foo-corp.com"]
   * @param options.domainData - The domains associated with the organization, including verification state.
   * @param options.stripeCustomerId - The Stripe customer ID associated with the organization.
   * @example "cus_R9qWAGMQ6nGE7V"
   * @param options.metadata - Object containing [metadata](https://workos.com/docs/authkit/metadata) key/value pairs associated with the Organization.
   * @example {"tier":"diamond"}
   * @param options.externalId - An external identifier for the Organization.
   * @example "2fe01467-f7ea-4dd2-8b79-c2b4f56d0191"
   * @returns {Promise<Organization>}
   * @throws {BadRequestException} 400
   * @throws {AuthorizationException} 403
   * @throws {NotFoundException} 404
   * @throws {ConflictException} 409
   * @throws {UnprocessableEntityException} 422
   */
  async updateOrganization(
    options: UpdateOrganizationOptions,
  ): Promise<Organization> {
    const { id, ...payload } = options;
    const { data } = await this.workos.put<
      OrganizationResponse,
      UpdateOrganizationResponse
    >(
      `/organizations/${encodeURIComponent(id)}`,
      serializeUpdateOrganization(payload),
    );
    return deserializeOrganization(data);
  }

  /**
   * Delete an Organization
   *
   * Permanently deletes an organization in the current environment. It cannot be undone.
   * @param options - The request options.
   * @param options.id - Unique identifier of the Organization.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @returns {Promise<void>}
   * @throws {AuthorizationException} 403
   */
  async deleteOrganization(options: DeleteOrganizationOptions): Promise<void> {
    const { id } = options;
    await this.workos.delete(`/organizations/${encodeURIComponent(id)}`);
  }

  /**
   * Get Audit Log Configuration
   *
   * Get the unified view of audit log trail and stream configuration for an organization.
   * @param options - The request options.
   * @param options.id - Unique identifier of the Organization.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @returns {Promise<AuditLogConfiguration>}
   * @throws {NotFoundException} 404
   */
  async getAuditLogConfiguration(
    options: GetAuditLogConfigurationOptions,
  ): Promise<AuditLogConfiguration> {
    const { id } = options;
    const { data } = await this.workos.get<AuditLogConfigurationResponse>(
      `/organizations/${encodeURIComponent(id)}/audit_log_configuration`,
    );
    return deserializeAuditLogConfiguration(data);
  }
}
