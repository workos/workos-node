// This file is auto-generated by oagen. Do not edit.

import fetch from 'jest-fetch-mock';
import {
  fetchOnce,
  fetchURL,
  fetchMethod,
  fetchSearchParams,
  fetchBody,
} from '../common/utils/test-utils';
import { WorkOS } from '../workos';

import createDataKeyResponseFixture from './fixtures/create-data-key-response.json';
import decryptResponseFixture from './fixtures/decrypt-response.json';
import listObjectSummaryFixture from './fixtures/list-object-summary.json';
import objectMetadataFixture from './fixtures/object-metadata.json';
import vaultObjectFixture from './fixtures/vault-object.json';
import objectWithoutValueFixture from './fixtures/object-without-value.json';
import versionListResponseFixture from './fixtures/version-list-response.json';

const workos = new WorkOS('sk_test_Sz3IQjepeSWaI4cMS4ms4sMuU');

function expectCreateDataKeyResponse(result: any) {
  expect(result.context).toEqual({
    organization_id: 'org_01K8ZYT4AWJ6XP0E0S8CTBHE3P',
  });
  expect(result.dataKey).toBe('DR9idtey9MpMrA1VRFrz30HB1yNgL2PoHZyjAkFeWgg=');
  expect(result.encryptedKeys).toBe(
    'V09TLkVLTS52MQBiZjUxY2NlYy03OGI0LTUyMDAtYjM4My0zNTczMGU3MWVmNjEBATEBJGJmNjVlMzI2LTQzYTAtNGIyMC04OGM0LTA3ZmYzZGU1NDM0YwF0YmY2NWUzMjYtNDNhMC00YjIwLTg4YzQtMDdmZjNkZTU0MzRj',
  );
  expect(result.id).toBe('bf51ccec-78b4-5200-b383-35730e71ef61');
}

function expectVaultObject(result: any) {
  expect(result.id).toBe('a1b2c3d4-e5f6-7890-abcd-ef1234567890');
  expect(result.name).toBe('my-secret');
  expect(result.value).toBe('s3cr3t-v4lu3');
}

function expectObjectWithoutValue(result: any) {
  expect(result.id).toBe('a1b2c3d4-e5f6-7890-abcd-ef1234567890');
  expect(result.name).toBe('my-secret');
}

describe('Vault', () => {
  beforeEach(() => fetch.resetMocks());

  describe('createDataKey', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(createDataKeyResponseFixture);

      const result = await workos.vault.createDataKey({} as any);

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/vault/v1/keys/data-key',
      );
      expect(fetchBody()).toBeDefined();
      expect(result).toBeDefined();
    });
  });

  describe('decryptDataKey', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(decryptResponseFixture);

      const result = await workos.vault.decryptDataKey({ keys: 'test_keys' });

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/vault/v1/keys/decrypt',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({ keys: 'test_keys' }),
      );
      expect(result).toBeDefined();
    });
  });

  describe('createRekey', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(createDataKeyResponseFixture);

      const result = await workos.vault.createRekey({} as any);

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe('/vault/v1/keys/rekey');
      expect(fetchBody()).toBeDefined();
      expectCreateDataKeyResponse(result);
    });
  });

  describe('listObjects', () => {
    it('returns paginated results', async () => {
      fetchOnce(listObjectSummaryFixture);

      const { data, listMetadata } = await workos.vault.listObjects({
        order: 'desc',
        search: 'test_search',
        updatedAfter: new Date('2023-01-01T00:00:00.000Z'),
      });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe('/vault/v1/kv');
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
      expect(data[0].id).toBe('a1b2c3d4-e5f6-7890-abcd-ef1234567890');
      expect(data[0].name).toBe('my-secret');
    });
  });

  describe('createObject', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(objectMetadataFixture);

      const result = await workos.vault.createObject({} as any);

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe('/vault/v1/kv');
      expect(fetchBody()).toBeDefined();
      expect(result.context).toEqual({
        organization_id: 'org_01K8ZYT4AWJ6XP0E0S8CTBHE3P',
      });
      expect(result.environmentId).toBe(
        'environment_01K8ZYT4AWJ6XP0E0S8CTBHE3P',
      );
      expect(result.id).toBe('a1b2c3d4-e5f6-7890-abcd-ef1234567890');
      expect(result.keyId).toBe('bf65e326-43a0-4b20-88c4-07ff3de5434c');
      expect(result.updatedAt.toISOString()).toBe('2024-06-15T10:30:00.000Z');
    });
  });

  describe('readObjectByName', () => {
    it('returns the expected result', async () => {
      fetchOnce(vaultObjectFixture);

      const result = await workos.vault.readObjectByName({ name: 'test_name' });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/vault/v1/kv/name/test_name',
      );
      expectVaultObject(result);
    });
  });

  describe('readObject', () => {
    it('returns the expected result', async () => {
      fetchOnce(vaultObjectFixture);

      const result = await workos.vault.readObject({ id: 'test_id' });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe('/vault/v1/kv/test_id');
      expectVaultObject(result);
    });
  });

  describe('updateObject', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(objectWithoutValueFixture);

      const result = await workos.vault.updateObject({
        id: 'test_id',
        value: 'test_value',
      });

      expect(fetchMethod()).toBe('PUT');
      expect(new URL(String(fetchURL())).pathname).toBe('/vault/v1/kv/test_id');
      expect(fetchBody()).toEqual(
        expect.objectContaining({ value: 'test_value' }),
      );
      expect(result).toBeDefined();
    });
  });

  describe('deleteObject', () => {
    it('sends a DELETE request', async () => {
      fetchOnce({}, { status: 204 });

      await workos.vault.deleteObject({
        id: 'test_id',
        versionCheck: 'test_version_check',
      });

      expect(fetchMethod()).toBe('DELETE');
      expect(new URL(String(fetchURL())).pathname).toBe('/vault/v1/kv/test_id');
    });
  });

  describe('describeObject', () => {
    it('returns the expected result', async () => {
      fetchOnce(objectWithoutValueFixture);

      const result = await workos.vault.describeObject({ id: 'test_id' });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/vault/v1/kv/test_id/metadata',
      );
      expectObjectWithoutValue(result);
    });
  });

  describe('listObjectVersions', () => {
    it('returns the expected result', async () => {
      fetchOnce(versionListResponseFixture);

      const result = await workos.vault.listObjectVersions({ id: 'test_id' });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/vault/v1/kv/test_id/versions',
      );
      expect(Array.isArray(result)).toBe(true);
    });
  });
});
