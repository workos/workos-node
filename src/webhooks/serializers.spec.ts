// This file is auto-generated by oagen. Do not edit.

import { serializeCreateWebhookEndpoint } from './serializers/create-webhook-endpoint.serializer';
import { serializeUpdateWebhookEndpoint } from './serializers/update-webhook-endpoint.serializer';
import { deserializeWebhookEndpoint } from './serializers/webhook-endpoint.serializer';
import type { CreateWebhookEndpointResponse } from './interfaces/create-webhook-endpoint.interface';
import type { UpdateWebhookEndpointResponse } from './interfaces/update-webhook-endpoint.interface';
import type { WebhookEndpointResponse } from './interfaces/webhook-endpoint.interface';
import createWebhookEndpointFixture from './fixtures/create-webhook-endpoint.json';
import updateWebhookEndpointFixture from './fixtures/update-webhook-endpoint.json';
import webhookEndpointFixture from './fixtures/webhook-endpoint.json';

describe('CreateWebhookEndpointSerializer', () => {
  it('serializes correctly', () => {
    const fixture =
      createWebhookEndpointFixture as CreateWebhookEndpointResponse;
    const serialized = serializeCreateWebhookEndpoint(fixture as any);
    expect(serialized).toBeDefined();
  });
});

describe('UpdateWebhookEndpointSerializer', () => {
  it('serializes correctly', () => {
    const fixture =
      updateWebhookEndpointFixture as UpdateWebhookEndpointResponse;
    const serialized = serializeUpdateWebhookEndpoint(fixture as any);
    expect(serialized).toBeDefined();
  });
});

describe('WebhookEndpointSerializer', () => {
  it('deserializes correctly', () => {
    const fixture = webhookEndpointFixture as WebhookEndpointResponse;
    const deserialized = deserializeWebhookEndpoint(fixture);
    expect(deserialized).toBeDefined();
  });
});
