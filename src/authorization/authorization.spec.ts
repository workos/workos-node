// This file is auto-generated by oagen. Do not edit.

import fetch from 'jest-fetch-mock';
import {
  fetchOnce,
  fetchURL,
  fetchMethod,
  fetchSearchParams,
  fetchBody,
} from '../common/utils/test-utils';
import { WorkOS } from '../workos';

import authorizationCheckFixture from './fixtures/authorization-check.json';
import listAuthorizationResourceFixture from './fixtures/list-authorization-resource.json';
import listAuthorizationPermissionFixture from './fixtures/list-authorization-permission.json';
import listUserRoleAssignmentFixture from './fixtures/list-user-role-assignment.json';
import roleAssignmentFixture from '../common/fixtures/role-assignment.json';
import roleListFixture from '../roles/fixtures/role-list.json';
import organizationRoleFixture from '../common/fixtures/organization-role.json';
import authorizationResourceFixture from './fixtures/authorization-resource.json';
import listUserOrganizationMembershipBaseListDataFixture from './fixtures/list-user-organization-membership-base-list-data.json';
import environmentRoleFixture from '../common/fixtures/environment-role.json';
import permissionFixture from './fixtures/permission.json';
import authorizationPermissionFixture from './fixtures/authorization-permission.json';

const workos = new WorkOS('sk_test_Sz3IQjepeSWaI4cMS4ms4sMuU');

function expectAuthorizationResource(result: any) {
  expect(result.object).toBe('authorization_resource');
  expect(result.name).toBe('Website Redesign');
  expect(result.description).toBe('Company website redesign project');
  expect(result.organizationId).toBe('org_01EHZNVPK3SFK441A1RGBFSHRT');
  expect(result.parentResourceId).toBe(
    'authz_resource_01HXYZ123456789ABCDEFGHIJ',
  );
  expect(result.id).toBe('authz_resource_01HXYZ123456789ABCDEFGH');
  expect(result.externalId).toBe('proj-456');
  expect(result.resourceTypeSlug).toBe('project');
  expect(result.createdAt.toISOString()).toBe('2026-01-15T12:00:00.000Z');
  expect(result.updatedAt.toISOString()).toBe('2026-01-15T12:00:00.000Z');
}

function expectAuthorizationPermission(result: any) {
  expect(result.object).toBe('permission');
  expect(result.id).toBe('perm_01HXYZ123456789ABCDEFGHIJ');
  expect(result.slug).toBe('documents:read');
  expect(result.name).toBe('View Documents');
  expect(result.description).toBe('Allows viewing document contents');
  expect(result.system).toBe(false);
  expect(result.resourceTypeSlug).toBe('workspace');
  expect(result.createdAt.toISOString()).toBe('2026-01-15T12:00:00.000Z');
  expect(result.updatedAt.toISOString()).toBe('2026-01-15T12:00:00.000Z');
}

function expectRoleList(result: any) {
  expect(result.object).toBe('list');
}

describe('Authorization', () => {
  beforeEach(() => fetch.resetMocks());

  describe('check', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(authorizationCheckFixture);

      const result = await workos.authorization.check({
        organizationMembershipId: 'test_organizationMembershipId',
        permissionSlug: 'test_permission_slug',
      });

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organization_memberships/test_organizationMembershipId/check',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({ permission_slug: 'test_permission_slug' }),
      );
      expect(result.authorized).toBe(true);
    });
  });

  describe('listResourcesForMembership', () => {
    it('returns paginated results', async () => {
      fetchOnce(listAuthorizationResourceFixture);

      const { data, listMetadata } =
        await workos.authorization.listResourcesForMembership({
          organizationMembershipId: 'test_organizationMembershipId',
          order: 'desc',
          permissionSlug: 'project:read',
          parentResourceId: 'authz_resource_01XYZ789',
          parentResourceTypeSlug: 'project',
          parentResourceExternalId: 'external_project_123',
        });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organization_memberships/test_organizationMembershipId/resources',
      );
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
      expectAuthorizationResource(data[0]);
    });
  });

  describe('listEffectivePermissions', () => {
    it('returns paginated results', async () => {
      fetchOnce(listAuthorizationPermissionFixture);

      const { data, listMetadata } =
        await workos.authorization.listEffectivePermissions({
          organizationMembershipId: 'test_organizationMembershipId',
          resourceId: 'test_resourceId',
          order: 'desc',
        });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organization_memberships/test_organizationMembershipId/resources/test_resourceId/permissions',
      );
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
    });
  });

  describe('listEffectivePermissionsByExternalId', () => {
    it('returns paginated results', async () => {
      fetchOnce(listAuthorizationPermissionFixture);

      const { data, listMetadata } =
        await workos.authorization.listEffectivePermissionsByExternalId({
          organizationMembershipId: 'test_organizationMembershipId',
          resourceTypeSlug: 'test_resourceTypeSlug',
          externalId: 'test_externalId',
          order: 'desc',
        });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organization_memberships/test_organizationMembershipId/resources/test_resourceTypeSlug/test_externalId/permissions',
      );
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
    });
  });

  describe('listRoleAssignments', () => {
    it('returns paginated results', async () => {
      fetchOnce(listUserRoleAssignmentFixture);

      const { data, listMetadata } =
        await workos.authorization.listRoleAssignments({
          organizationMembershipId: 'test_organizationMembershipId',
          order: 'desc',
          resourceId: 'authz_resource_01HXYZ123456789ABCDEFGH',
          resourceExternalId: 'project-ext-456',
          resourceTypeSlug: 'project',
        });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organization_memberships/test_organizationMembershipId/role_assignments',
      );
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
    });
  });

  describe('assignRole', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(roleAssignmentFixture);

      const result = await workos.authorization.assignRole({
        organizationMembershipId: 'test_organizationMembershipId',
        roleSlug: 'test_role_slug',
      });

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organization_memberships/test_organizationMembershipId/role_assignments',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({ role_slug: 'test_role_slug' }),
      );
      expect(result).toBeDefined();
    });
  });

  describe('removeRole', () => {
    it('sends a DELETE request', async () => {
      fetchOnce({}, { status: 204 });

      await workos.authorization.removeRole({
        organizationMembershipId: 'test_organizationMembershipId',
        roleSlug: 'test_role_slug',
      });

      expect(fetchMethod()).toBe('DELETE');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organization_memberships/test_organizationMembershipId/role_assignments',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({ role_slug: 'test_role_slug' }),
      );
    });
  });

  describe('removeRoleAssignment', () => {
    it('sends a DELETE request', async () => {
      fetchOnce({}, { status: 204 });

      await workos.authorization.removeRoleAssignment({
        organizationMembershipId: 'test_organizationMembershipId',
        roleAssignmentId: 'test_roleAssignmentId',
      });

      expect(fetchMethod()).toBe('DELETE');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organization_memberships/test_organizationMembershipId/role_assignments/test_roleAssignmentId',
      );
    });
  });

  describe('listOrganizationRoles', () => {
    it('returns the expected result', async () => {
      fetchOnce(roleListFixture);

      const result = await workos.authorization.listOrganizationRoles({
        organizationId: 'test_organizationId',
      });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organizations/test_organizationId/roles',
      );
      expectRoleList(result);
    });
  });

  describe('createOrganizationRole', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(organizationRoleFixture);

      const result = await workos.authorization.createOrganizationRole({
        organizationId: 'test_organizationId',
        name: 'Test',
      });

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organizations/test_organizationId/roles',
      );
      expect(fetchBody()).toEqual(expect.objectContaining({ name: 'Test' }));
      expect(result).toBeDefined();
    });
  });

  describe('getOrganizationRole', () => {
    it('returns the expected result', async () => {
      fetchOnce(organizationRoleFixture);

      const result = await workos.authorization.getOrganizationRole({
        organizationId: 'test_organizationId',
        slug: 'test_slug',
      });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organizations/test_organizationId/roles/test_slug',
      );
      expect(result).toBeDefined();
    });
  });

  describe('updateOrganizationRole', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(organizationRoleFixture);

      const result = await workos.authorization.updateOrganizationRole({
        organizationId: 'test_organizationId',
        slug: 'test_slug',
        name: 'Test',
      });

      expect(fetchMethod()).toBe('PATCH');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organizations/test_organizationId/roles/test_slug',
      );
      expect(fetchBody()).toEqual(expect.objectContaining({ name: 'Test' }));
      expect(result).toBeDefined();
    });
  });

  describe('deleteOrganizationRole', () => {
    it('sends a DELETE request', async () => {
      fetchOnce({}, { status: 204 });

      await workos.authorization.deleteOrganizationRole({
        organizationId: 'test_organizationId',
        slug: 'test_slug',
      });

      expect(fetchMethod()).toBe('DELETE');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organizations/test_organizationId/roles/test_slug',
      );
    });
  });

  describe('addOrganizationRolePermission', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(organizationRoleFixture);

      const result = await workos.authorization.addOrganizationRolePermission({
        organizationId: 'test_organizationId',
        roleSlug: 'test_slug',
        slug: 'test_slug',
      });

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organizations/test_organizationId/roles/test_slug/permissions',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({ slug: 'test_slug' }),
      );
      expect(result).toBeDefined();
    });
  });

  describe('setOrganizationRolePermissions', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(organizationRoleFixture);

      const result = await workos.authorization.setOrganizationRolePermissions({
        organizationId: 'test_organizationId',
        slug: 'test_slug',
        permissions: ['test_permissions'],
      });

      expect(fetchMethod()).toBe('PUT');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organizations/test_organizationId/roles/test_slug/permissions',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({ permissions: ['test_permissions'] }),
      );
      expect(result).toBeDefined();
    });
  });

  describe('removeOrganizationRolePermission', () => {
    it('sends a DELETE request', async () => {
      fetchOnce({}, { status: 204 });

      await workos.authorization.removeOrganizationRolePermission({
        organizationId: 'test_organizationId',
        slug: 'test_slug',
        permissionSlug: 'test_permissionSlug',
      });

      expect(fetchMethod()).toBe('DELETE');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organizations/test_organizationId/roles/test_slug/permissions/test_permissionSlug',
      );
    });
  });

  describe('getResourceByExternalId', () => {
    it('returns the expected result', async () => {
      fetchOnce(authorizationResourceFixture);

      const result = await workos.authorization.getResourceByExternalId({
        organizationId: 'test_organizationId',
        resourceTypeSlug: 'test_resourceTypeSlug',
        externalId: 'test_externalId',
      });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organizations/test_organizationId/resources/test_resourceTypeSlug/test_externalId',
      );
      expectAuthorizationResource(result);
    });
  });

  describe('updateResourceByExternalId', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(authorizationResourceFixture);

      const result = await workos.authorization.updateResourceByExternalId({
        organizationId: 'test_organizationId',
        resourceTypeSlug: 'test_resourceTypeSlug',
        externalId: 'test_externalId',
        name: 'Test',
        parentResourceId: 'parent_resource_id_01234',
      });

      expect(fetchMethod()).toBe('PATCH');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organizations/test_organizationId/resources/test_resourceTypeSlug/test_externalId',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({
          name: 'Test',
          parent_resource_id: 'parent_resource_id_01234',
        }),
      );
      expectAuthorizationResource(result);
    });
  });

  describe('deleteResourceByExternalId', () => {
    it('sends a DELETE request', async () => {
      fetchOnce({}, { status: 204 });

      await workos.authorization.deleteResourceByExternalId({
        organizationId: 'test_organizationId',
        resourceTypeSlug: 'test_resourceTypeSlug',
        externalId: 'test_externalId',
        cascadeDelete: false,
      });

      expect(fetchMethod()).toBe('DELETE');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organizations/test_organizationId/resources/test_resourceTypeSlug/test_externalId',
      );
    });
  });

  describe('listMembershipsForResourceByExternalId', () => {
    it('returns paginated results', async () => {
      fetchOnce(listUserOrganizationMembershipBaseListDataFixture);

      const { data, listMetadata } =
        await workos.authorization.listMembershipsForResourceByExternalId({
          organizationId: 'test_organizationId',
          resourceTypeSlug: 'test_resourceTypeSlug',
          externalId: 'test_externalId',
          order: 'desc',
          permissionSlug: 'project:read',
          assignment: 'direct',
        });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organizations/test_organizationId/resources/test_resourceTypeSlug/test_externalId/organization_memberships',
      );
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
    });
  });

  describe('listResourceRoleAssignments', () => {
    it('returns paginated results', async () => {
      fetchOnce(listUserRoleAssignmentFixture);

      const { data, listMetadata } =
        await workos.authorization.listResourceRoleAssignments({
          organizationId: 'test_organizationId',
          resourceTypeSlug: 'test_resourceTypeSlug',
          externalId: 'test_externalId',
          order: 'desc',
          roleSlug: 'editor',
        });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/organizations/test_organizationId/resources/test_resourceTypeSlug/test_externalId/role_assignments',
      );
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
    });
  });

  describe('listResources', () => {
    it('returns paginated results', async () => {
      fetchOnce(listAuthorizationResourceFixture);

      const { data, listMetadata } = await workos.authorization.listResources({
        order: 'desc',
        organizationId: 'org_01EHZNVPK3SFK441A1RGBFSHRT',
        resourceTypeSlug: 'project',
        resourceExternalId: 'my-project-123',
        parentResourceId: 'authz_resource_01HXYZ123456789ABCDEFGHIJ',
        parentResourceTypeSlug: 'workspace',
        parentExternalId: 'ext-workspace-123',
      });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/resources',
      );
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
      expectAuthorizationResource(data[0]);
    });
  });

  describe('createResource', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(authorizationResourceFixture);

      const result = await workos.authorization.createResource({
        externalId: 'external_id_01234',
        name: 'Test',
        resourceTypeSlug: 'test_resource_type_slug',
        organizationId: 'organization_id_01234',
      });

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/resources',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({
          external_id: 'external_id_01234',
          name: 'Test',
          resource_type_slug: 'test_resource_type_slug',
          organization_id: 'organization_id_01234',
        }),
      );
      expectAuthorizationResource(result);
    });
  });

  describe('getResource', () => {
    it('returns the expected result', async () => {
      fetchOnce(authorizationResourceFixture);

      const result = await workos.authorization.getResource({
        resourceId: 'test_resourceId',
      });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/resources/test_resourceId',
      );
      expectAuthorizationResource(result);
    });
  });

  describe('updateResource', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(authorizationResourceFixture);

      const result = await workos.authorization.updateResource({
        resourceId: 'test_resourceId',
        name: 'Test',
        parentResourceId: 'parent_resource_id_01234',
      });

      expect(fetchMethod()).toBe('PATCH');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/resources/test_resourceId',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({
          name: 'Test',
          parent_resource_id: 'parent_resource_id_01234',
        }),
      );
      expectAuthorizationResource(result);
    });
  });

  describe('deleteResource', () => {
    it('sends a DELETE request', async () => {
      fetchOnce({}, { status: 204 });

      await workos.authorization.deleteResource({
        resourceId: 'test_resourceId',
        cascadeDelete: false,
      });

      expect(fetchMethod()).toBe('DELETE');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/resources/test_resourceId',
      );
    });
  });

  describe('listMembershipsForResource', () => {
    it('returns paginated results', async () => {
      fetchOnce(listUserOrganizationMembershipBaseListDataFixture);

      const { data, listMetadata } =
        await workos.authorization.listMembershipsForResource({
          resourceId: 'test_resourceId',
          order: 'desc',
          permissionSlug: 'document:edit',
          assignment: 'direct',
        });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/resources/test_resourceId/organization_memberships',
      );
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
    });
  });

  describe('listRoleAssignmentsForResource', () => {
    it('returns paginated results', async () => {
      fetchOnce(listUserRoleAssignmentFixture);

      const { data, listMetadata } =
        await workos.authorization.listRoleAssignmentsForResource({
          resourceId: 'test_resourceId',
          order: 'desc',
          roleSlug: 'editor',
        });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/resources/test_resourceId/role_assignments',
      );
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
    });
  });

  describe('listEnvironmentRoles', () => {
    it('returns the expected result', async () => {
      fetchOnce(roleListFixture);

      const result = await workos.authorization.listEnvironmentRoles();

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe('/authorization/roles');
      expectRoleList(result);
    });
  });

  describe('createEnvironmentRole', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(environmentRoleFixture);

      const result = await workos.authorization.createEnvironmentRole({
        slug: 'test_slug',
        name: 'Test',
      });

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe('/authorization/roles');
      expect(fetchBody()).toEqual(
        expect.objectContaining({ slug: 'test_slug', name: 'Test' }),
      );
      expect(result).toBeDefined();
    });
  });

  describe('getEnvironmentRole', () => {
    it('returns the expected result', async () => {
      fetchOnce(environmentRoleFixture);

      const result = await workos.authorization.getEnvironmentRole({
        slug: 'test_slug',
      });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/roles/test_slug',
      );
      expect(result).toBeDefined();
    });
  });

  describe('updateEnvironmentRole', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(environmentRoleFixture);

      const result = await workos.authorization.updateEnvironmentRole({
        slug: 'test_slug',
        name: 'Test',
      });

      expect(fetchMethod()).toBe('PATCH');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/roles/test_slug',
      );
      expect(fetchBody()).toEqual(expect.objectContaining({ name: 'Test' }));
      expect(result).toBeDefined();
    });
  });

  describe('addEnvironmentRolePermission', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(environmentRoleFixture);

      const result = await workos.authorization.addEnvironmentRolePermission({
        roleSlug: 'test_slug',
        slug: 'test_slug',
      });

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/roles/test_slug/permissions',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({ slug: 'test_slug' }),
      );
      expect(result).toBeDefined();
    });
  });

  describe('setEnvironmentRolePermissions', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(environmentRoleFixture);

      const result = await workos.authorization.setEnvironmentRolePermissions({
        slug: 'test_slug',
        permissions: ['test_permissions'],
      });

      expect(fetchMethod()).toBe('PUT');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/roles/test_slug/permissions',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({ permissions: ['test_permissions'] }),
      );
      expect(result).toBeDefined();
    });
  });

  describe('listPermissions', () => {
    it('returns paginated results', async () => {
      fetchOnce(listAuthorizationPermissionFixture);

      const { data, listMetadata } = await workos.authorization.listPermissions(
        { order: 'desc' },
      );

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/permissions',
      );
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
    });
  });

  describe('createPermission', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(permissionFixture);

      const result = await workos.authorization.createPermission({
        slug: 'test_slug',
        name: 'Test',
      });

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/permissions',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({ slug: 'test_slug', name: 'Test' }),
      );
      expect(result.object).toBe('permission');
      expect(result.id).toBe('perm_01HXYZ123456789ABCDEFGHIJ');
      expect(result.slug).toBe('documents:read');
      expect(result.name).toBe('View Documents');
      expect(result.description).toBe('Allows viewing document contents');
      expect(result.system).toBe(false);
      expect(result.resourceTypeSlug).toBe('document');
      expect(result.createdAt.toISOString()).toBe('2026-01-15T12:00:00.000Z');
      expect(result.updatedAt.toISOString()).toBe('2026-01-15T12:00:00.000Z');
    });
  });

  describe('getPermission', () => {
    it('returns the expected result', async () => {
      fetchOnce(authorizationPermissionFixture);

      const result = await workos.authorization.getPermission({
        slug: 'test_slug',
      });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/permissions/test_slug',
      );
      expectAuthorizationPermission(result);
    });
  });

  describe('updatePermission', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(authorizationPermissionFixture);

      const result = await workos.authorization.updatePermission({
        slug: 'test_slug',
        name: 'Test',
      });

      expect(fetchMethod()).toBe('PATCH');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/permissions/test_slug',
      );
      expect(fetchBody()).toEqual(expect.objectContaining({ name: 'Test' }));
      expectAuthorizationPermission(result);
    });
  });

  describe('deletePermission', () => {
    it('sends a DELETE request', async () => {
      fetchOnce({}, { status: 204 });

      await workos.authorization.deletePermission({ slug: 'test_slug' });

      expect(fetchMethod()).toBe('DELETE');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/authorization/permissions/test_slug',
      );
    });
  });
});
