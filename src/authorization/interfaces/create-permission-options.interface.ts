export interface CreatePermissionOptions {
  slug: string;
  name: string;
  description?: string;
}

export interface SerializedCreatePermissionOptions {
  slug: string;
  name: string;
  description?: string;
}
