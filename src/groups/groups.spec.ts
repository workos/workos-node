// This file is auto-generated by oagen. Do not edit.

import fetch from 'jest-fetch-mock';
import {
  fetchOnce,
  fetchURL,
  fetchMethod,
  fetchSearchParams,
  fetchBody,
} from '../common/utils/test-utils';
import { WorkOS } from '../workos';

import listGroupFixture from './fixtures/list-group.json';
import groupFixture from './fixtures/group.json';
import listUserOrganizationMembershipBaseListDataFixture from './fixtures/list-user-organization-membership-base-list-data.json';

const workos = new WorkOS('sk_test_Sz3IQjepeSWaI4cMS4ms4sMuU');

function expectGroup(result: any) {
  expect(result.object).toBe('group');
  expect(result.id).toBe('group_01HXYZ123456789ABCDEFGHIJ');
  expect(result.organizationId).toBe('org_01EHWNCE74X7JSDV0X3SZ3KJNY');
  expect(result.name).toBe('Engineering');
  expect(result.description).toBe('The engineering team');
  expect(result.createdAt.toISOString()).toBe('2026-01-15T12:00:00.000Z');
  expect(result.updatedAt.toISOString()).toBe('2026-01-15T12:00:00.000Z');
}

describe('Groups', () => {
  beforeEach(() => fetch.resetMocks());

  describe('listGroups', () => {
    it('returns paginated results', async () => {
      fetchOnce(listGroupFixture);

      const { data, listMetadata } = await workos.groups.listGroups({
        organizationId: 'test_organizationId',
        order: 'desc',
      });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/organizations/test_organizationId/groups',
      );
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
      expectGroup(data[0]);
    });
  });

  describe('createGroup', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(groupFixture);

      const result = await workos.groups.createGroup({
        organizationId: 'test_organizationId',
        name: 'Test',
      });

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/organizations/test_organizationId/groups',
      );
      expect(fetchBody()).toEqual(expect.objectContaining({ name: 'Test' }));
      expectGroup(result);
    });
  });

  describe('getGroup', () => {
    it('returns the expected result', async () => {
      fetchOnce(groupFixture);

      const result = await workos.groups.getGroup({
        organizationId: 'test_organizationId',
        groupId: 'test_groupId',
      });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/organizations/test_organizationId/groups/test_groupId',
      );
      expectGroup(result);
    });
  });

  describe('updateGroup', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(groupFixture);

      const result = await workos.groups.updateGroup({
        organizationId: 'test_organizationId',
        groupId: 'test_groupId',
        name: 'Test',
      });

      expect(fetchMethod()).toBe('PATCH');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/organizations/test_organizationId/groups/test_groupId',
      );
      expect(fetchBody()).toEqual(expect.objectContaining({ name: 'Test' }));
      expectGroup(result);
    });
  });

  describe('deleteGroup', () => {
    it('sends a DELETE request', async () => {
      fetchOnce({}, { status: 204 });

      await workos.groups.deleteGroup({
        organizationId: 'test_organizationId',
        groupId: 'test_groupId',
      });

      expect(fetchMethod()).toBe('DELETE');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/organizations/test_organizationId/groups/test_groupId',
      );
    });
  });

  describe('listOrganizationMemberships', () => {
    it('returns paginated results', async () => {
      fetchOnce(listUserOrganizationMembershipBaseListDataFixture);

      const { data, listMetadata } =
        await workos.groups.listOrganizationMemberships({
          organizationId: 'test_organizationId',
          groupId: 'test_groupId',
          order: 'desc',
        });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/organizations/test_organizationId/groups/test_groupId/organization-memberships',
      );
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
    });
  });

  // @oagen-ignore-start
  describe('listOrganizationMemberships', () => {
    it('returns paginated results', async () => {
      fetchOnce(listUserOrganizationMembershipBaseListDataFixture);

      const { data, listMetadata } =
        await workos.groups.listOrganizationMemberships({
          organizationId: 'test_organizationId',
          groupId: 'test_groupId',
          order: 'desc',
        });

      expect(fetchMethod()).toBe('GET');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/organizations/test_organizationId/groups/test_groupId/organization-memberships',
      );
      expect(fetchSearchParams()).toHaveProperty('order');
      expect(Array.isArray(data)).toBe(true);
      expect(listMetadata).toBeDefined();
      expect(data.length).toBeGreaterThan(0);
      expect(data[0].object).toBe('organization_membership');
      expect(data[0].id).toBe('om_01HXYZ123456789ABCDEFGHIJ');
      expect(data[0].userId).toBe('user_01E4ZCR3C56J083X43JQXF3JK5');
      expect(data[0].organizationId).toBe('org_01EHZNVPK3SFK441A1RGBFSHRT');
      expect(data[0].status).toBe('active');
      expect(data[0].directoryManaged).toBe(false);
      expect(data[0].createdAt).toBe('2026-01-15T12:00:00.000Z');
      expect(data[0].updatedAt).toBe('2026-01-15T12:00:00.000Z');
      expect(data[0].customAttributes).toEqual({
        department: 'Engineering',
        title: 'Developer Experience Engineer',
        location: 'Brooklyn',
      });
      expect(data[0]).not.toHaveProperty('user');
    });
  });
  // @oagen-ignore-end
  describe('addOrganizationMembership', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(groupFixture);

      const result = await workos.groups.addOrganizationMembership({
        organizationId: 'test_organizationId',
        groupId: 'test_groupId',
        organizationMembershipId: 'organization_membership_id_01234',
      });

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/organizations/test_organizationId/groups/test_groupId/organization-memberships',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({
          organization_membership_id: 'organization_membership_id_01234',
        }),
      );
      expectGroup(result);
    });
  });

  describe('removeOrganizationMembership', () => {
    it('sends a DELETE request', async () => {
      fetchOnce({}, { status: 204 });

      await workos.groups.removeOrganizationMembership({
        organizationId: 'test_organizationId',
        groupId: 'test_groupId',
        organizationMembershipId: 'test_omId',
      });

      expect(fetchMethod()).toBe('DELETE');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/organizations/test_organizationId/groups/test_groupId/organization-memberships/test_omId',
      );
    });
  });
});
