// This file is auto-generated by oagen. Do not edit.

import fetch from 'jest-fetch-mock';
import {
  fetchOnce,
  fetchURL,
  fetchMethod,
  fetchBody,
  testUnauthorized,
} from '../common/utils/test-utils';
import { WorkOS } from '../workos';

import radarStandaloneResponseFixture from './fixtures/radar-standalone-response.fixture.json';
import radarListEntryAlreadyPresentResponseFixture from './fixtures/radar-list-entry-already-present-response.fixture.json';

const workos = new WorkOS('sk_test_Sz3IQjepeSWaI4cMS4ms4sMuU');

describe('Radar', () => {
  beforeEach(() => fetch.resetMocks());

  describe('assess', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(radarStandaloneResponseFixture);

      const result = await workos.radar.assess({
        ipAddress: 'test_ip_address',
        userAgent: 'test_user_agent',
        email: 'test@example.com',
        authMethod: 'Password',
        action: 'login',
      });

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe('/radar/attempts');
      expect(fetchBody()).toEqual(
        expect.objectContaining({
          ip_address: 'test_ip_address',
          user_agent: 'test_user_agent',
          email: 'test@example.com',
          auth_method: 'Password',
          action: 'login',
        }),
      );
      expect(result.verdict).toBe('block');
      expect(result.reason).toBe('Detected enabled Radar control');
      expect(result.attemptId).toBe('radar_att_01HZBC6N1EB1ZY7KG32X');
    });

    testUnauthorized(() =>
      workos.radar.assess({
        ipAddress: 'test_ip_address',
        userAgent: 'test_user_agent',
        email: 'test@example.com',
        authMethod: 'Password',
        action: 'login',
      }),
    );
  });

  describe('updateRadarAttempt', () => {
    it('sends the request', async () => {
      fetchOnce({});

      await workos.radar.updateRadarAttempt('test_id', {});

      expect(fetchMethod()).toBe('PUT');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/radar/attempts/test_id',
      );
    });
  });

  describe('updateRadarList', () => {
    it('sends the correct request and returns result', async () => {
      fetchOnce(radarListEntryAlreadyPresentResponseFixture);

      const result = await workos.radar.updateRadarList('ip_address', 'block', {
        entry: 'test_entry',
      });

      expect(fetchMethod()).toBe('POST');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/radar/lists/ip_address/block',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({ entry: 'test_entry' }),
      );
      expect(result.message).toBe('Entry already present in list');
    });

    testUnauthorized(() =>
      workos.radar.updateRadarList('ip_address', 'block', {
        entry: 'test_entry',
      }),
    );
  });

  describe('deleteRadarListEntry', () => {
    it('sends a DELETE request', async () => {
      fetchOnce({}, { status: 204 });

      await workos.radar.deleteRadarListEntry('ip_address', 'block', {
        entry: 'test_entry',
      });

      expect(fetchMethod()).toBe('DELETE');
      expect(new URL(String(fetchURL())).pathname).toBe(
        '/radar/lists/ip_address/block',
      );
      expect(fetchBody()).toEqual(
        expect.objectContaining({ entry: 'test_entry' }),
      );
    });
  });
});
