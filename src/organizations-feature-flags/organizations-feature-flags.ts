// This file is auto-generated by oagen. Do not edit.

import type { WorkOS } from '../workos';
import type { PaginationOptions } from '../common/interfaces/pagination-options.interface';
import type { AutoPaginatable } from '../common/utils/pagination';
import { createPaginatedList } from '../common/utils/fetch-and-deserialize';
import type {
  Flag,
  FlagResponse,
} from '../feature-flags/interfaces/flag.interface';
import { deserializeFlag } from '../feature-flags/serializers/flag.serializer';

export class OrganizationsFeatureFlags {
  constructor(private readonly workos: WorkOS) {}

  /**
   * List enabled feature flags for an organization
   *
   * Get a list of all enabled feature flags for an organization.
   * @param organizationId - Unique identifier of the Organization.
   * @example "org_01EHZNVPK3SFK441A1RGBFSHRT"
   * @param options - Pagination and filter options.
   * @returns {AutoPaginatable<Flag>}
   * @throws {NotFoundException} 404
   */
  async list(
    organizationId: string,
    options?: PaginationOptions,
  ): Promise<AutoPaginatable<Flag, PaginationOptions>> {
    return createPaginatedList<FlagResponse, Flag, PaginationOptions>(
      this.workos,
      `/organizations/${organizationId}/feature-flags`,
      deserializeFlag,
      options,
    );
  }
}
