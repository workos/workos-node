// This file is auto-generated by oagen. Do not edit.

import type {
  ConnectApplicationRedirectUri,
  ConnectApplicationRedirectUriResponse,
} from './connect-application-redirect-uri.interface';

export interface ConnectApplicationOAuth {
  /** Distinguishes the connect application object. */
  object: 'connect_application';
  /** The unique ID of the connect application. */
  id: string;
  /** The client ID of the connect application. */
  clientId: string;
  /** A description of the connect application. */
  description: string | null;
  /** The name of the connect application. */
  name: string;
  /** The scopes available for this application. */
  scopes: string[];
  /** An ISO 8601 timestamp. */
  createdAt: Date;
  /** An ISO 8601 timestamp. */
  updatedAt: Date;
  /** The type of the application. */
  applicationType: 'oauth';
  /** The redirect URIs configured for this application. */
  redirectUris: ConnectApplicationRedirectUri[];
  /** Whether the application uses PKCE for authorization. */
  usesPkce: boolean;
  /** Whether the application is a first-party application. */
  isFirstParty: boolean;
  /** Whether the application was dynamically registered. */
  wasDynamicallyRegistered?: boolean;
  /** The ID of the organization the application belongs to. */
  organizationId?: string;
}

export interface ConnectApplicationOAuthResponse {
  /** Distinguishes the connect application object. */
  object: 'connect_application';
  /** The unique ID of the connect application. */
  id: string;
  /** The client ID of the connect application. */
  client_id: string;
  /** A description of the connect application. */
  description: string | null;
  /** The name of the connect application. */
  name: string;
  /** The scopes available for this application. */
  scopes: string[];
  /** An ISO 8601 timestamp. */
  created_at: string;
  /** An ISO 8601 timestamp. */
  updated_at: string;
  /** The type of the application. */
  application_type: 'oauth';
  /** The redirect URIs configured for this application. */
  redirect_uris: ConnectApplicationRedirectUriResponse[];
  /** Whether the application uses PKCE for authorization. */
  uses_pkce: boolean;
  /** Whether the application is a first-party application. */
  is_first_party: boolean;
  /** Whether the application was dynamically registered. */
  was_dynamically_registered?: boolean;
  /** The ID of the organization the application belongs to. */
  organization_id?: string;
}

export interface ConnectApplicationM2M {
  /** Distinguishes the connect application object. */
  object: 'connect_application';
  /** The unique ID of the connect application. */
  id: string;
  /** The client ID of the connect application. */
  clientId: string;
  /** A description of the connect application. */
  description: string | null;
  /** The name of the connect application. */
  name: string;
  /** The scopes available for this application. */
  scopes: string[];
  /** An ISO 8601 timestamp. */
  createdAt: Date;
  /** An ISO 8601 timestamp. */
  updatedAt: Date;
  /** The type of the application. */
  applicationType: 'm2m';
  /** The ID of the organization the application belongs to. */
  organizationId: string;
}

export interface ConnectApplicationM2MResponse {
  /** Distinguishes the connect application object. */
  object: 'connect_application';
  /** The unique ID of the connect application. */
  id: string;
  /** The client ID of the connect application. */
  client_id: string;
  /** A description of the connect application. */
  description: string | null;
  /** The name of the connect application. */
  name: string;
  /** The scopes available for this application. */
  scopes: string[];
  /** An ISO 8601 timestamp. */
  created_at: string;
  /** An ISO 8601 timestamp. */
  updated_at: string;
  /** The type of the application. */
  application_type: 'm2m';
  /** The ID of the organization the application belongs to. */
  organization_id: string;
}

export type ConnectApplication =
  | ConnectApplicationOAuth
  | ConnectApplicationM2M;

export type ConnectApplicationResponse =
  | ConnectApplicationOAuthResponse
  | ConnectApplicationM2MResponse;
