// This file is auto-generated by oagen. Do not edit.

import { deserializeUserObject, serializeUserObject } from './serializers/user-object.serializer';
import { deserializeUserConsentOption, serializeUserConsentOption } from './serializers/user-consent-option.serializer';
import { deserializeUserManagementLoginRequest, serializeUserManagementLoginRequest } from './serializers/user-management-login-request.serializer';
import { deserializeRedirectUriInput, serializeRedirectUriInput } from './serializers/redirect-uri-input.serializer';
import { deserializeCreateOAuthApplication, serializeCreateOAuthApplication } from './serializers/create-oauth-application.serializer';
import { deserializeCreateM2MApplication, serializeCreateM2MApplication } from './serializers/create-m2m-application.serializer';
import { deserializeUpdateOAuthApplication, serializeUpdateOAuthApplication } from './serializers/update-oauth-application.serializer';
import { deserializeExternalAuthCompleteResponse, serializeExternalAuthCompleteResponse } from './serializers/external-auth-complete-response.serializer';
import { deserializeConnectApplication, serializeConnectApplication } from './serializers/connect-application.serializer';
import { deserializeNewConnectApplicationSecret, serializeNewConnectApplicationSecret } from './serializers/new-connect-application-secret.serializer';
import { deserializeUserConsentOptionChoice, serializeUserConsentOptionChoice } from './serializers/user-consent-option-choice.serializer';
import { deserializeApplicationCredentialsListItem, serializeApplicationCredentialsListItem } from './serializers/application-credentials-list-item.serializer';
import type { UserObjectResponse } from './interfaces/user-object.interface';
import type { UserConsentOptionResponse } from './interfaces/user-consent-option.interface';
import type { UserManagementLoginRequestResponse } from './interfaces/user-management-login-request.interface';
import type { RedirectUriInputResponse } from './interfaces/redirect-uri-input.interface';
import type { CreateOAuthApplicationResponse } from './interfaces/create-oauth-application.interface';
import type { CreateM2MApplicationResponse } from './interfaces/create-m2m-application.interface';
import type { UpdateOAuthApplicationResponse } from './interfaces/update-oauth-application.interface';
import type { ExternalAuthCompleteResponseWire } from './interfaces/external-auth-complete-response.interface';
import type { ConnectApplicationResponse } from './interfaces/connect-application.interface';
import type { NewConnectApplicationSecretResponse } from './interfaces/new-connect-application-secret.interface';
import type { UserConsentOptionChoiceResponse } from './interfaces/user-consent-option-choice.interface';
import type { ApplicationCredentialsListItemResponse } from './interfaces/application-credentials-list-item.interface';
import userObjectFixture from './fixtures/user-object.fixture.json';
import userConsentOptionFixture from './fixtures/user-consent-option.fixture.json';
import userManagementLoginRequestFixture from './fixtures/user-management-login-request.fixture.json';
import redirectUriInputFixture from './fixtures/redirect-uri-input.fixture.json';
import createOAuthApplicationFixture from './fixtures/create-oauth-application.fixture.json';
import createM2MApplicationFixture from './fixtures/create-m2m-application.fixture.json';
import updateOAuthApplicationFixture from './fixtures/update-oauth-application.fixture.json';
import externalAuthCompleteResponseFixture from './fixtures/external-auth-complete-response.fixture.json';
import connectApplicationFixture from './fixtures/connect-application.fixture.json';
import newConnectApplicationSecretFixture from './fixtures/new-connect-application-secret.fixture.json';
import userConsentOptionChoiceFixture from './fixtures/user-consent-option-choice.fixture.json';
import applicationCredentialsListItemFixture from './fixtures/application-credentials-list-item.fixture.json';

describe('UserObjectSerializer', () => {
  it('round-trips through serialize/deserialize', () => {
    const fixture = userObjectFixture as UserObjectResponse;
    const deserialized = deserializeUserObject(fixture);
    const reserialized = serializeUserObject(deserialized);
    expect(reserialized).toEqual(expect.objectContaining(fixture));
  });
});

describe('UserConsentOptionSerializer', () => {
  it('round-trips through serialize/deserialize', () => {
    const fixture = userConsentOptionFixture as UserConsentOptionResponse;
    const deserialized = deserializeUserConsentOption(fixture);
    const reserialized = serializeUserConsentOption(deserialized);
    expect(reserialized).toEqual(expect.objectContaining(fixture));
  });
});

describe('UserManagementLoginRequestSerializer', () => {
  it('round-trips through serialize/deserialize', () => {
    const fixture = userManagementLoginRequestFixture as UserManagementLoginRequestResponse;
    const deserialized = deserializeUserManagementLoginRequest(fixture);
    const reserialized = serializeUserManagementLoginRequest(deserialized);
    expect(reserialized).toEqual(expect.objectContaining(fixture));
  });
});

describe('RedirectUriInputSerializer', () => {
  it('round-trips through serialize/deserialize', () => {
    const fixture = redirectUriInputFixture as RedirectUriInputResponse;
    const deserialized = deserializeRedirectUriInput(fixture);
    const reserialized = serializeRedirectUriInput(deserialized);
    expect(reserialized).toEqual(expect.objectContaining(fixture));
  });
});

describe('CreateOAuthApplicationSerializer', () => {
  it('round-trips through serialize/deserialize', () => {
    const fixture = createOAuthApplicationFixture as CreateOAuthApplicationResponse;
    const deserialized = deserializeCreateOAuthApplication(fixture);
    const reserialized = serializeCreateOAuthApplication(deserialized);
    expect(reserialized).toEqual(expect.objectContaining(fixture));
  });
});

describe('CreateM2MApplicationSerializer', () => {
  it('round-trips through serialize/deserialize', () => {
    const fixture = createM2MApplicationFixture as CreateM2MApplicationResponse;
    const deserialized = deserializeCreateM2MApplication(fixture);
    const reserialized = serializeCreateM2MApplication(deserialized);
    expect(reserialized).toEqual(expect.objectContaining(fixture));
  });
});

describe('UpdateOAuthApplicationSerializer', () => {
  it('round-trips through serialize/deserialize', () => {
    const fixture = updateOAuthApplicationFixture as UpdateOAuthApplicationResponse;
    const deserialized = deserializeUpdateOAuthApplication(fixture);
    const reserialized = serializeUpdateOAuthApplication(deserialized);
    expect(reserialized).toEqual(expect.objectContaining(fixture));
  });
});

describe('ExternalAuthCompleteResponseSerializer', () => {
  it('round-trips through serialize/deserialize', () => {
    const fixture = externalAuthCompleteResponseFixture as ExternalAuthCompleteResponseWire;
    const deserialized = deserializeExternalAuthCompleteResponse(fixture);
    const reserialized = serializeExternalAuthCompleteResponse(deserialized);
    expect(reserialized).toEqual(expect.objectContaining(fixture));
  });
});

describe('ConnectApplicationSerializer', () => {
  it('round-trips through serialize/deserialize', () => {
    const fixture = connectApplicationFixture as ConnectApplicationResponse;
    const deserialized = deserializeConnectApplication(fixture);
    const reserialized = serializeConnectApplication(deserialized);
    expect(reserialized).toEqual(expect.objectContaining(fixture));
  });
});

describe('NewConnectApplicationSecretSerializer', () => {
  it('round-trips through serialize/deserialize', () => {
    const fixture = newConnectApplicationSecretFixture as NewConnectApplicationSecretResponse;
    const deserialized = deserializeNewConnectApplicationSecret(fixture);
    const reserialized = serializeNewConnectApplicationSecret(deserialized);
    expect(reserialized).toEqual(expect.objectContaining(fixture));
  });
});

describe('UserConsentOptionChoiceSerializer', () => {
  it('round-trips through serialize/deserialize', () => {
    const fixture = userConsentOptionChoiceFixture as UserConsentOptionChoiceResponse;
    const deserialized = deserializeUserConsentOptionChoice(fixture);
    const reserialized = serializeUserConsentOptionChoice(deserialized);
    expect(reserialized).toEqual(expect.objectContaining(fixture));
  });
});

describe('ApplicationCredentialsListItemSerializer', () => {
  it('round-trips through serialize/deserialize', () => {
    const fixture = applicationCredentialsListItemFixture as ApplicationCredentialsListItemResponse;
    const deserialized = deserializeApplicationCredentialsListItem(fixture);
    const reserialized = serializeApplicationCredentialsListItem(deserialized);
    expect(reserialized).toEqual(expect.objectContaining(fixture));
  });
});
